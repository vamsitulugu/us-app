diff --git a/android/app/src/main/java/com/uswithlove/app/MainActivity.java b/android/app/src/main/java/com/uswithlove/app/MainActivity.java
index b8ea144..593e3a8 100644
--- a/android/app/src/main/java/com/uswithlove/app/MainActivity.java
+++ b/android/app/src/main/java/com/uswithlove/app/MainActivity.java
@@ -112,6 +112,14 @@ public class MainActivity extends BridgeActivity {
     if (path == null || path.isEmpty()) return;
     String target = path.startsWith("http") ? path : APP_ORIGIN + path;
     boolean isCallAnswer = path.contains("pendingAction=answer");
+    // CALL FAST PATH: a plain tap on the incoming-call notification body
+    // (routes/call.js tags the url with launch=call) is just as
+    // reload-sensitive as the explicit Answer action — the call is
+    // already ringing inside the running JS via call.js's realtime
+    // channel/poll loop, so a full webView.loadUrl() here would tear
+    // that down and force the entire skeleton/init pipeline to run
+    // again just to show a call that's already active.
+    boolean isCallLaunch = isCallAnswer || path.contains("launch=call");
 
     android.webkit.WebView webView = getBridge() != null ? getBridge().getWebView() : null;
 
@@ -122,6 +130,11 @@ public class MainActivity extends BridgeActivity {
       if (isCallAnswer) {
         webView.post(() -> webView.evaluateJavascript(
             "window.Call && window.Call.consumeNativeAnswer && window.Call.consumeNativeAnswer();", null));
+      } else if (isCallLaunch) {
+        // Not an auto-answer — just make sure Chat (where the incoming-call
+        // UI lives) is the visible page, without reloading anything.
+        webView.post(() -> webView.evaluateJavascript(
+            "window.goto && window.goto('chat');", null));
       } else {
         webView.post(() -> webView.loadUrl(target));
       }
diff --git a/public/chat/chat.js b/public/chat/chat.js
index a4bbf9a..0d1d173 100644
--- a/public/chat/chat.js
+++ b/public/chat/chat.js
@@ -349,8 +349,8 @@ function reanchorAfterImages() {
     const mine = isMine(m);
     const time = new Date(m.created_at).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
     let body = '';
-    if (m.type === 'image') body = `<img src="${esc(m.media_url)}" class="chat-img" onclick="openImgViewer('${esc(m.media_url)}')" loading="lazy">`;
-    else if (m.type === 'gif') body = `<img src="${esc(m.media_url)}" class="chat-img chat-gif" onclick="openImgViewer('${esc(m.media_url)}')" loading="lazy">`;
+    if (m.type === 'image') body = `<img src="${esc(m.media_url)}" class="chat-img" onclick="Chat.openMediaViewer('${m.id}')" loading="lazy">`;
+    else if (m.type === 'gif') body = `<img src="${esc(m.media_url)}" class="chat-img chat-gif" onclick="Chat.openMediaViewer('${m.id}')" loading="lazy">`;
     else if (m.type === 'voice') body = renderVoice(m);
     else if (m.type === 'audio') body = `<audio controls src="${esc(m.media_url)}" style="max-width:220px"></audio>`;
     else if (m.type === 'location') {
@@ -430,14 +430,32 @@ function reanchorAfterImages() {
 
   function renderVoice(m) {
     const dur = (m.media_meta && m.media_meta.duration) || 0;
-    return `<div class="voice-msg" onclick="event.stopPropagation();Chat.toggleVoicePlay(this,'${esc(m.media_url)}')">
-      <button class="voice-play">▶</button>
+    // IMPORTANT: playback must only be triggered by the play/pause button
+    // itself. The outer .voice-msg container intentionally has NO click
+    // handler of its own — a click anywhere else (waveform, duration,
+    // padding) is left to bubble up to the normal .chat-row bubble click
+    // handler (Chat.onBubbleClick), which drives selection-mode
+    // tap-to-select and does nothing in normal mode. This is what stops
+    // "tap bubble -> plays audio" while still letting long-press/selection
+    // work anywhere on the bubble, including the waveform.
+    return `<div class="voice-msg">
+      <button type="button" class="voice-play" aria-label="Play voice message" onclick="event.stopPropagation();Chat.toggleVoicePlay(this.parentElement,'${esc(m.media_url)}')">▶</button>
       <div class="voice-waveform">${Array.from({length:18}).map((_,i)=>`<span style="height:${8+Math.random()*16}px"></span>`).join('')}</div>
       <div class="voice-dur">${Math.floor(dur/60)}:${String(dur%60).padStart(2,'0')}</div>
     </div>`;
   }
   let _activeVoiceEl = null;
   function toggleVoicePlay(el, url) {
+    // Selection mode takes priority: tapping Play while in WhatsApp-style
+    // multi-select mode must select/deselect the message rather than
+    // start audio (the click never reaches onBubbleClick because it's
+    // stopped in the inline handler above, so replicate the toggle here).
+    if (selectMode) {
+      const row = el.closest('.chat-row');
+      const id = row && row.getAttribute('data-id');
+      if (id != null) toggleSelect(id);
+      return;
+    }
     let audio = el._audio;
     if (!audio) { audio = new Audio(url); el._audio = audio; audio.onended = () => { el.querySelector('.voice-waveform').classList.remove('playing'); el.querySelector('.voice-play').textContent = '▶'; if (_activeVoiceEl === el) _activeVoiceEl = null; }; }
     if (audio.paused) {
@@ -454,6 +472,18 @@ function reanchorAfterImages() {
     else { audio.pause(); el.querySelector('.voice-waveform').classList.remove('playing'); el.querySelector('.voice-play').textContent = '▶'; if (_activeVoiceEl === el) _activeVoiceEl = null; }
   }
 
+  // Opens the app-wide gallery viewer (public/js/gallery-viewer.js via
+  // openImgViewer in index.html) scoped to this chat's photos/gifs —
+  // swiping moves between the actual photos in the conversation,
+  // starting at whichever one was tapped, not always photo #1.
+  function openMediaViewer(id) {
+    const media = msgs.filter(m => !m.deleted && (m.type === 'image' || m.type === 'gif'));
+    const idx = media.findIndex(m => String(m.id) === String(id));
+    if (idx === -1) return;
+    const collection = media.map(m => ({ url: m.media_url, type: 'image' }));
+    if (window.openImgViewer) window.openImgViewer(media[idx].media_url, collection, idx);
+  }
+
   function renderPinned() {
     const bar = document.getElementById('chatPinnedBar');
     if (!bar) return;
@@ -1590,7 +1620,7 @@ function menuItemsHtml(m, id, includeSelect) {
     onChatScroll, scrollToBottom, sendText, onTypingInput, onImagePick, toggleRecord,
     onBubbleClick, openMenu, reactTo, replyTo, closeBanner, togglePin, toggleStar,
     openStarred, deleteMsg, confirmDeleteMsg, enterSelectMode, deleteSelected, exitSelectMode,
-    isSelecting, closeMsgMenuIfOpen, openToolbarOverflow,
+    isSelecting, closeMsgMenuIfOpen, openToolbarOverflow, openMediaViewer,
     openSearch, closeSearch, runSearch, scrollToMsg, sendGif, sendEmoji, sendEmojiTap,
     openEmojiPanel, switchEmojiTab, filterEmoji, openGifPanel, searchGifs, markRead, init, openSheet, closeSheet,
     forwardMsg, copyMsg, editMsg, cancelEdit, infoMsg, cancelRecording, startLongPress, endLongPress, moveLongPress,
diff --git a/public/index.html b/public/index.html
index ec13940..f26e2a4 100644
--- a/public/index.html
+++ b/public/index.html
@@ -233,8 +233,34 @@
   .al-auth-wrap.al-show { display: flex; }
   /* Only one skeleton on screen at a time: once the auth skeleton is
      shown, fully hide the dashboard/sidebar skeleton behind it instead
-     of letting both render simultaneously. */
+     of letting both render simultaneously. Same rule for call mode. */
   #appLoader.al-auth-mode .al-shell { display: none; }
+  #appLoader.al-call-mode .al-shell { display: none; }
+  /* CALL FAST PATH: a cold/backgrounded launch from an incoming-call
+     notification never shows the generic dashboard/sidebar skeleton —
+     just this tiny centered "connecting" card, so there's nothing
+     unrelated (Memory tiles, nav items, metrics) flashing on screen
+     while the call signaling/media negotiation is still in flight. */
+  #appLoader.al-call-mode .al-progress {
+    position: static;
+    transform: none;
+    margin: 0 auto;
+    flex-direction: column;
+    gap: 14px;
+    top: 50%;
+  }
+  #appLoader.al-call-mode {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+  }
+  #appLoader.al-call-mode .al-progress-dot {
+    width: 14px; height: 14px;
+  }
+  #appLoader.al-call-mode #alProgressText {
+    font-size: 15px;
+    color: rgba(255,255,255,0.85);
+  }
   .al-auth-card {
     width: 100%;
     max-width: 460px;
@@ -2288,25 +2314,29 @@
       z-index: 9998;
       background: rgba(0, 0, 0, 0.95);
       display: none;
-      align-items: center;
-      justify-content: center;
       backdrop-filter: blur(4px)
     }
 
     .img-viewer.open {
-      display: flex
+      display: block
     }
 
-    .img-viewer img {
-      max-width: 92vw;
-      max-height: 92dvh;
-      object-fit: contain;
-      border-radius: 10px
+    /* Fixed UI overlay — always a sibling of .img-viewer-stage, always
+       positioned relative to the viewport, never affected by any
+       swipe/zoom/pan transform applied inside the stage below it. */
+    .img-viewer-overlay {
+      position: fixed;
+      inset: 0;
+      z-index: 2;
+      pointer-events: none;
+      padding-top: env(safe-area-inset-top, 0px);
     }
 
+    .img-viewer-overlay > * { pointer-events: auto; }
+
     .img-viewer-close {
       position: absolute;
-      top: calc(18px + env(safe-area-inset-top, 0px));
+      top: 18px;
       right: 22px;
       color: var(--white);
       font-size: 24px;
@@ -2314,19 +2344,79 @@
       background: rgba(255, 255, 255, 0.12);
       border: none;
       border-radius: 50%;
-      width: 42px;
-      height: 42px;
+      width: 44px;
+      height: 44px;
       display: flex;
       align-items: center;
       justify-content: center;
       transition: var(--t);
-      z-index: 1
     }
 
     .img-viewer-close:hover {
       background: rgba(255, 255, 255, 0.25)
     }
 
+    .img-viewer-counter {
+      position: absolute;
+      top: 26px;
+      left: 50%;
+      transform: translateX(-50%);
+      color: #eee;
+      font-size: 13px;
+      font-weight: 600;
+      text-shadow: 0 1px 3px rgba(0,0,0,.6);
+    }
+
+    /* Gallery stage: the ONLY layer the GalleryEngine writes/transforms
+       into. Sits below the fixed overlay in stacking order but is a
+       plain sibling in the DOM, not an ancestor of the overlay — so
+       there is no transformed-ancestor path from the close button
+       back to any zoomed/panned/swiping content. */
+    .img-viewer-stage,
+    .mem-viewer-stage {
+      position: relative;
+      z-index: 1;
+      width: 100%;
+      height: 100%;
+      overflow: hidden;
+      touch-action: pan-y;
+    }
+
+    /* ── Shared GalleryEngine layers (gallery-viewer.js) ────────── */
+    .gve-track {
+      position: relative;
+      width: 100%;
+      height: 100%;
+      will-change: transform;
+    }
+    .gve-slide {
+      position: absolute;
+      inset: 0;
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      will-change: transform;
+    }
+    .gve-content {
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      width: 100%;
+      height: 100%;
+      will-change: transform;
+      touch-action: none;
+    }
+    .gve-content img,
+    .gve-content video {
+      max-width: 92vw;
+      max-height: 92dvh;
+      object-fit: contain;
+      border-radius: 10px;
+      user-select: none;
+      -webkit-user-drag: none;
+      pointer-events: none; /* drag/zoom handled by the wrapping .gve-content */
+    }
+
     /* ════ MEMORIES GALLERY VIEWER ════
        Dedicated full-screen viewer for the Memories page only — separate
        from the shared .img-viewer above (which Chat/Vault/etc. still use
@@ -2928,8 +3018,8 @@
     }
 
     .voice-play {
-      width: 32px;
-      height: 32px;
+      width: 44px;
+      height: 44px;
       border-radius: 50%;
       background: rgba(255, 255, 255, 0.2);
       border: none;
@@ -2940,6 +3030,8 @@
       font-size: 14px;
       flex-shrink: 0;
       transition: var(--t);
+      /* This is intentionally the ONLY clickable control inside .voice-msg
+         — see chat.js renderVoice()/toggleVoicePlay() for why. */
     }
 
     .voice-play:hover {
@@ -4686,6 +4778,7 @@ select option { background: #0D0D0D; color: #fff; }
   <link rel="stylesheet" href="/safe-delete.css">
   <script src="/js/scroll-reset.js"></script>
   <script src="/js/session-manager.js"></script>
+  <script src="/js/gallery-viewer.js"></script>
   <script src="/safe-delete.js"></script>
 </head>
 
@@ -4830,9 +4923,20 @@ select option { background: #0D0D0D; color: #fff; }
       if (overlay) overlay.classList.remove('al-auth-mode');
     },
     // escape hatch, e.g. for a "Retry" button on network failure
-    forceHide: hideOverlay
+    forceHide: hideOverlay,
+    // CALL FAST PATH: swap the generic sidebar+dashboard skeleton for a
+    // minimal "Connecting…" card. Called synchronously (see the
+    // ?launch=call detector near the top of the page script) as soon as
+    // a call launch is known — well before auth/data would normally be
+    // ready — and the overlay is force-hidden the moment coupleId is
+    // known (see finishRestore's CALL FAST PATH branch), so this is
+    // only ever on screen for a very brief, genuinely-loading window.
+    showCallSkeleton(type) {
+      if (!overlay) return;
+      overlay.classList.add('al-call-mode');
+      if (progressText) progressText.textContent = (type === 'video' ? 'Video call' : 'Voice call') + ' connecting…';
+    }
   };
- 
   // Fonts: resolve automatically via the Font Loading API.
   if (document.fonts && document.fonts.ready) {
     document.fonts.ready.then(() => window.AppLoader.ready('fonts')).catch(() => window.AppLoader.ready('fonts'));
@@ -4861,10 +4965,19 @@ select option { background: #0D0D0D; color: #fff; }
   <!-- MOBILE SIDEBAR OVERLAY -->
   <div class="sidebar-overlay" id="sbOverlay" onclick="closeMobSidebar()"></div>
 
-  <!-- IMAGE VIEWER -->
+  <!-- IMAGE VIEWER — app-wide swipeable gallery (chat media, etc).
+       Structure is intentionally flat: the fixed UI overlay (close
+       button + counter) is a SIBLING of the gve-stage the
+       GalleryEngine renders/transforms into, never a descendant of
+       it. Nothing the engine does (swipe track transform, per-slide
+       zoom/pan transform) can ever move, hide, or clip this overlay,
+       regardless of image size/orientation/zoom level. -->
   <div class="img-viewer" id="imgViewer">
-    <button class="img-viewer-close" onclick="closeImgViewer()">✕</button>
-    <img id="imgViewerImg" src="" alt="">
+    <div class="img-viewer-overlay gve-no-drag">
+      <button class="img-viewer-close" onclick="closeImgViewer()" aria-label="Close"><i data-lucide="x"></i></button>
+      <div class="img-viewer-counter" id="imgViewerCounter"></div>
+    </div>
+    <div class="img-viewer-stage" id="imgViewerStage"></div>
   </div>
 
   <!-- MEMORIES GALLERY VIEWER (dedicated to the Memories page — see Part below) -->
@@ -4875,10 +4988,8 @@ select option { background: #0D0D0D; color: #fff; }
       <div style="width:42px"></div>
     </div>
     <div class="mem-viewer-stage" id="memViewerStage">
-      <img id="memViewerImg" src="" alt="memory" style="display:none">
-      <video id="memViewerVideo" controls playsinline style="display:none"></video>
-      <button type="button" class="mem-viewer-nav prev" id="memViewerPrevBtn" onclick="memViewerPrev()" aria-label="Previous"><i data-lucide="chevron-left"></i></button>
-      <button type="button" class="mem-viewer-nav next" id="memViewerNextBtn" onclick="memViewerNext()" aria-label="Next"><i data-lucide="chevron-right"></i></button>
+      <button type="button" class="mem-viewer-nav prev gve-no-drag" id="memViewerPrevBtn" onclick="memViewerPrev()" aria-label="Previous"><i data-lucide="chevron-left"></i></button>
+      <button type="button" class="mem-viewer-nav next gve-no-drag" id="memViewerNextBtn" onclick="memViewerNext()" aria-label="Next"><i data-lucide="chevron-right"></i></button>
     </div>
     <div class="mem-viewer-bar" id="memViewerBar">
       <button type="button" class="mem-viewer-action fav" id="memViewerFavBtn" onclick="memViewerToggleFav()">
@@ -6934,6 +7045,18 @@ select option { background: #0D0D0D; color: #fff; }
   // before anything else runs, so a fully-closed-app cold start doesn't
   // lose it to timing.
   window.__DEEP_LINK_PAGE__ = new URLSearchParams(window.location.search).get('page') || null;
+  // ── CALL FAST PATH: detect launch reason as early as the architecture
+  // allows (before Supabase client setup, before any auth network call).
+  // See routes/call.js — the incoming-call push URL is tagged
+  // ?launch=call&ct=voice|video specifically so this is a synchronous
+  // parse, not something that has to wait on any async signal.
+  (function () {
+    const qp = new URLSearchParams(window.location.search);
+    if (qp.get('launch') === 'call') {
+      window.__CALL_LAUNCH__ = { type: qp.get('ct') || 'voice' };
+      if (window.AppLoader) window.AppLoader.showCallSkeleton(window.__CALL_LAUNCH__.type);
+    }
+  })();
   window.__SUPABASE_URL__ = 'https://jmhsyhpmuszyphwjfcuk.supabase.co';
   window.__SUPABASE_ANON_KEY__ = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImptaHN5aHBtdXN6eXBod2pmY3VrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM5NzAzODIsImV4cCI6MjA5OTU0NjM4Mn0.hXmVIoESJ5SkbhIaN-UyWLbJ4XdvZD_dd7U2WzFfvNI';
   // Single shared Supabase client for this page's window context. chat.js,
@@ -7861,9 +7984,49 @@ document.querySelectorAll('.bot-ni[data-page]').forEach(el => {
         function spawnPetals() { }
 
         // ─── IMAGE VIEWER ────────────────────────────────────────
-        function openImgViewer(url) { document.getElementById('imgViewerImg').src = url; document.getElementById('imgViewer').classList.add('open'); }
-        function closeImgViewer() { document.getElementById('imgViewer').classList.remove('open'); }
-        document.getElementById('imgViewer').addEventListener('click', function (e) { if (e.target === this) closeImgViewer(); });
+        // App-wide image/gallery viewer. Backward compatible with the old
+        // single-argument openImgViewer(url) call sites (wraps it in a
+        // 1-item collection); callers with a real photo collection (e.g.
+        // Chat media) pass (url, collection, index) to get real
+        // swipe-between-photos behavior starting at the tapped photo.
+        let _imgViewerEngine = null;
+        function _imgViewerGetEngine() {
+          if (_imgViewerEngine) return _imgViewerEngine;
+          _imgViewerEngine = new GalleryEngine(document.getElementById('imgViewerStage'), {
+            renderItem(item) {
+              if (item.type === 'video') {
+                const v = document.createElement('video');
+                v.src = item.url; v.controls = true; v.playsInline = true;
+                return v;
+              }
+              const img = document.createElement('img');
+              img.src = item.url; img.alt = '';
+              return img;
+            },
+            onIndexChange(i, item) {
+              const list = _imgViewerEngine.items;
+              const counter = document.getElementById('imgViewerCounter');
+              counter.textContent = list.length > 1 ? (i + 1) + ' / ' + list.length : '';
+            },
+            onEscape() { closeImgViewer(); }
+          });
+          return _imgViewerEngine;
+        }
+        function openImgViewer(url, collection, index) {
+          const items = (collection && collection.length) ? collection : [{ url, type: 'image' }];
+          const startIndex = (collection && collection.length) ? (index || 0) : 0;
+          _imgViewerGetEngine().open(items, startIndex);
+          document.getElementById('imgViewer').classList.add('open');
+        }
+        function closeImgViewer() {
+          document.getElementById('imgViewer').classList.remove('open');
+          if (_imgViewerEngine) _imgViewerEngine.close();
+        }
+        // Tap the letterboxed background (not the photo itself, not a
+        // drag) to close — mirrors the old click-outside-image behavior.
+        document.getElementById('imgViewerStage').addEventListener('click', function (e) {
+          if (e.target.classList.contains('gve-content') || e.target.classList.contains('gve-slide') || e.target === this) closeImgViewer();
+        });
 
         // ─── NAMES / AVATARS ─────────────────────────────────────
         function applyNames() {
@@ -8529,9 +8692,29 @@ const p = await Notification.requestPermission();
            data model) and the existing favorite/delete/share functions
            above — this only adds the presentation layer + navigation. */
         let memViewerIndex = -1;
+        let _memViewerEngine = null;
 
         function memViewerList() { return S.photos || []; }
 
+        function _memViewerGetEngine() {
+          if (_memViewerEngine) return _memViewerEngine;
+          _memViewerEngine = new GalleryEngine(document.getElementById('memViewerStage'), {
+            renderItem(item) {
+              if (item.type === 'video') {
+                const v = document.createElement('video');
+                v.src = item.url; v.controls = true; v.playsInline = true;
+                return v;
+              }
+              const img = document.createElement('img');
+              img.src = item.url; img.alt = 'memory';
+              return img;
+            },
+            onIndexChange(i) { memViewerIndex = i; memViewerSyncChrome(); },
+            onEscape() { closeMemViewer(); }
+          });
+          return _memViewerEngine;
+        }
+
         function openMemViewer(id) {
           const list = memViewerList();
           const idx = list.findIndex(p => String(p.id) === String(id));
@@ -8539,13 +8722,13 @@ const p = await Notification.requestPermission();
           memViewerIndex = idx;
           document.getElementById('memViewer').classList.add('open');
           document.getElementById('memViewer').classList.remove('controls-hidden');
-          memViewerRender();
+          _memViewerGetEngine().open(list, idx);
+          memViewerSyncChrome();
         }
 
         function closeMemViewer() {
           document.getElementById('memViewer').classList.remove('open');
-          const v = document.getElementById('memViewerVideo');
-          if (v) { v.pause(); v.removeAttribute('src'); v.load(); }
+          if (_memViewerEngine) _memViewerEngine.close();
           memViewerIndex = -1;
         }
 
@@ -8554,21 +8737,13 @@ const p = await Notification.requestPermission();
           return memViewerIndex >= 0 && memViewerIndex < list.length ? list[memViewerIndex] : null;
         }
 
-        function memViewerRender() {
+        // Updates the top counter, chevron visibility and favorite icon
+        // to match the engine's current index — called on open and on
+        // every swipe/keyboard/button navigation via onIndexChange.
+        function memViewerSyncChrome() {
           const p = memViewerCurrent();
-          if (!p) { closeMemViewer(); return; }
+          if (!p) return;
           const list = memViewerList();
-          const img = document.getElementById('memViewerImg');
-          const vid = document.getElementById('memViewerVideo');
-          if (p.type === 'video') {
-            img.style.display = 'none'; img.src = '';
-            vid.style.display = '';
-            if (vid.src !== p.url) { vid.src = p.url; }
-          } else {
-            vid.pause(); vid.style.display = 'none'; vid.removeAttribute('src'); vid.load();
-            img.style.display = '';
-            img.src = p.url;
-          }
           document.getElementById('memViewerCounter').textContent = (memViewerIndex + 1) + ' / ' + list.length;
           document.getElementById('memViewerPrevBtn').style.visibility = memViewerIndex > 0 ? 'visible' : 'hidden';
           document.getElementById('memViewerNextBtn').style.visibility = memViewerIndex < list.length - 1 ? 'visible' : 'hidden';
@@ -8576,9 +8751,11 @@ const p = await Notification.requestPermission();
           favIcon.textContent = p.favorite ? '❤️' : '🤍';
           document.getElementById('memViewerFavBtn').classList.toggle('active', !!p.favorite);
         }
+        // Kept for backward compatibility with any other call sites.
+        function memViewerRender() { memViewerSyncChrome(); }
 
-        function memViewerPrev() { if (memViewerIndex > 0) { memViewerIndex--; memViewerRender(); } }
-        function memViewerNext() { const list = memViewerList(); if (memViewerIndex < list.length - 1) { memViewerIndex++; memViewerRender(); } }
+        function memViewerPrev() { _memViewerGetEngine().previous(); }
+        function memViewerNext() { _memViewerGetEngine().next(); }
 
         function memViewerToggleFav() {
           const p = memViewerCurrent();
@@ -8604,7 +8781,8 @@ const p = await Notification.requestPermission();
               const list = memViewerList();
               if (!list.length) { closeMemViewer(); return; }
               if (memViewerIndex >= list.length) memViewerIndex = list.length - 1;
-              memViewerRender();
+              _memViewerGetEngine().open(list, memViewerIndex);
+              memViewerSyncChrome();
             }
           });
         }
@@ -8658,31 +8836,12 @@ const p = await Notification.requestPermission();
           document.getElementById('memViewer').classList.toggle('controls-hidden');
         });
 
-        // Swipe left/right between memories, isolated from vertical scroll.
-        (function () {
-          const stage = document.getElementById('memViewerStage');
-          let sx = 0, sy = 0, tracking = false;
-          stage.addEventListener('pointerdown', (e) => {
-            if (e.target.closest('.mem-viewer-nav')) return;
-            sx = e.clientX; sy = e.clientY; tracking = true;
-          });
-          stage.addEventListener('pointerup', (e) => {
-            if (!tracking) return; tracking = false;
-            const dx = e.clientX - sx, dy = e.clientY - sy;
-            if (Math.abs(dx) > 60 && Math.abs(dx) > Math.abs(dy) * 1.5) {
-              if (dx < 0) memViewerNext(); else memViewerPrev();
-            }
-          });
-          stage.addEventListener('pointercancel', () => { tracking = false; });
-        })();
-
-        // Desktop keyboard navigation.
-        document.addEventListener('keydown', (e) => {
-          if (!document.getElementById('memViewer').classList.contains('open')) return;
-          if (e.key === 'ArrowLeft') memViewerPrev();
-          else if (e.key === 'ArrowRight') memViewerNext();
-          else if (e.key === 'Escape') closeMemViewer();
-        });
+        // Swipe, pinch/double-tap zoom+pan, and ←/→/Esc keyboard nav are
+        // all handled by GalleryEngine (see _memViewerGetEngine above).
+        // Its keydown listener only acts while this.stage is actually
+        // visible (see gallery-viewer.js), so it's inert while the
+        // viewer is closed and doesn't fight with the chat media viewer's
+        // own GalleryEngine instance.
 
         function renderMemoryAnalytics() {
           const insEl = document.getElementById('memInsightCard'), monEl = document.getElementById('memMonthCard');
@@ -10405,7 +10564,14 @@ if (window.Capacitor?.Plugins?.SplashScreen) {
             role: data.role || 'user1', paired: !!data.paired, setupDone: true
           });
           // Latest couple/profile/partner info (existing endpoint — no
-          // schema or auth changes).
+          // schema or auth changes). For a call launch, S.coupleId/role
+          // above are already everything call signaling needs — don't
+          // make the call UI wait on this unrelated fetch too.
+          if (window.__CALL_LAUNCH__) {
+            finishRestore();
+            loadFromCloud().then(saveLocal).catch(e => console.warn('[call-launch] deferred loadFromCloud failed', e));
+            return;
+          }
           await loadFromCloud();
           saveLocal();
           finishRestore();
@@ -10424,6 +10590,29 @@ if (window.Capacitor?.Plugins?.SplashScreen) {
             document.documentElement.style.setProperty('--h', h);
           }
           document.getElementById('authOverlay').style.display = 'none';
+
+          // ── CALL FAST PATH ──────────────────────────────────────────
+          // The moment we have what call signaling actually needs —
+          // coupleId + role, both already set on S above by restoreAccount()
+          // — get out of the way immediately: drop the skeleton and land on
+          // Chat (where call.js's existing ringing/incoming UI renders) right
+          // now, before paying for renderAll()'s full dashboard paint,
+          // ProfileStore, touch/miss-you/hug checks, alarm scanning, or any
+          // of the other unrelated work below. Everything else in this
+          // function still runs — Memory/Habits/etc. all still need it
+          // eventually — it just no longer blocks the call from appearing.
+          // call.js's own initSignalCursor()/pollSignal()/realtime channel
+          // (wired independently on DOMContentLoaded) picks the pending
+          // offer up and shows the incoming-call screen as soon as it's on
+          // screen to catch it.
+          if (window.__CALL_LAUNCH__) {
+            navigateToDeepLink('chat');
+            history.replaceState({}, '', window.location.pathname);
+            AppLoader.forceHide();
+            setTimeout(() => finishRestoreRemainder(), 0);
+            return;
+          }
+
           renderAll(); // now painting with fresh, validated state — never stale
           updatePairStatus();
           AppLoader.ready('data');
@@ -10436,6 +10625,16 @@ if (window.Capacitor?.Plugins?.SplashScreen) {
             window.__DEEP_LINK_PAGE__ = null;
             history.replaceState({}, '', window.location.pathname);
           }
+          finishRestoreRemainder();
+        }
+
+        // Everything in finishRestore() that ISN'T on the call fast path:
+        // dashboard-adjacent widgets, profile sync, presence, alarms, push
+        // registration prompts. Split out so the call-launch branch above
+        // can defer all of it (via setTimeout 0, i.e. next tick, after the
+        // call UI has already painted) instead of skipping it outright.
+        function finishRestoreRemainder() {
+          if (window.__CALL_LAUNCH__) { renderAll(); updatePairStatus(); AppLoader.ready('data'); }
           checkPairStatus();
           checkPendingPartnerRequest();
           subscribeToPartnerRealtime();
@@ -11418,8 +11617,17 @@ document.addEventListener('keydown', e => {
     try {
       const data = action?.notification?.data || {};
       if (!data.url) return;
-      const page = new URL(data.url, window.location.origin).searchParams.get('page');
+      const url = new URL(data.url, window.location.origin);
+      const page = url.searchParams.get('page');
       if (!page) return;
+      // CALL FAST PATH: this listener can fire after the synchronous
+      // ?launch=call parse at the top of the page already ran (e.g. app
+      // was warm and this arrives instead of a fresh navigation), so set
+      // the flag here too and, if the app is already up, jump straight
+      // to Chat instead of queuing a generic deep link.
+      if (url.searchParams.get('launch') === 'call') {
+        window.__CALL_LAUNCH__ = window.__CALL_LAUNCH__ || { type: url.searchParams.get('ct') || 'voice' };
+      }
       // On cold start this may fire before init() has rendered the
       // dashboard yet — queue it the same way the ?page= URL param is
       // queued, so it's picked up once the app finishes rendering.
diff --git a/routes/call.js b/routes/call.js
index 34be7de..e22e796 100644
--- a/routes/call.js
+++ b/routes/call.js
@@ -53,7 +53,11 @@ router.post('/notify', async (req, res) => {
     icon: '/icons/icon-192.png',
     tag: 'incoming-call',
     renotify: true,
-    url: '/?page=chat',
+    // launch=call is read synchronously at the very top of index.html's
+    // bootstrap (before any auth/session network calls) so the client
+    // can take the call fast-path instead of the normal full-app
+    // startup pipeline. See CALL FAST PATH in index.html.
+    url: '/?page=chat&launch=call&ct=' + (type || 'voice'),
     type: type || 'voice',
     senderName: callerName || undefined,
     senderAvatar: callerAvatar || undefined
