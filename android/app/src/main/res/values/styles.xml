<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- Legacy (API < 35) system bar theming. On API 35+ the OS
             forces edge-to-edge and ignores these; MainActivity.java
             handles that case natively via WindowInsetsControllerCompat. -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/statusBarBackground</item>
        <item name="android:navigationBarColor">@color/navigationBarBackground</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Was @drawable/splash — a raw per-density raster stretched/cropped
             to fill the window, which overflowed/broke the logo on screens
             whose aspect ratio didn't match the source image. Now a solid
             background with a fixed-size, always-centered logo that can
             never overflow (see drawable/splash_centered.xml). -->
        <item name="android:background">@drawable/splash_centered</item>
    </style>
</resources>