diff --git a/css-src/composer.css b/css-src/composer.css
index ff757f6..06f5b6e 100644
--- a/css-src/composer.css
+++ b/css-src/composer.css
@@ -179,6 +179,7 @@
   color: var(--white);
   transition: var(--t), transform 0.15s cubic-bezier(0.34, 1.56, 0.64, 1);
 }
+.uc-btn svg { width: 19px; height: 19px; }
 .uc-btn:hover { background: var(--g3); }
 .uc-btn:active { transform: scale(0.9); }
 
diff --git a/public/chat/chat.css b/public/chat/chat.css
index ecd7424..66de015 100644
--- a/public/chat/chat.css
+++ b/public/chat/chat.css
@@ -261,7 +261,8 @@
 }
 .chat-select-toolbar.active { display: flex; }
 .chat-select-toolbar .cnt { flex: 1; font-size: 13px; font-weight: 600; }
-.chat-select-toolbar button { background: rgba(255,255,255,0.2); border: none; color: #fff; border-radius: 10px; padding: 6px 12px; font-size: 12px; cursor: pointer; }
+.chat-select-toolbar button { background: rgba(255,255,255,0.2); border: none; color: #fff; border-radius: 10px; padding: 6px 12px; font-size: 12px; cursor: pointer; display: inline-flex; align-items: center; gap: 6px; }
+.chat-select-toolbar button svg { width: 16px; height: 16px; }
 
 /* ── CONTEXT MENU ── */
 .msg-ctx-bg { position: fixed; inset: 0; z-index: 9400; background: transparent; }
@@ -410,6 +411,11 @@
 
 .chat-rec-timer{font-size:11px;color:var(--red-status);font-weight:700}
 .chat-row{display:flex;margin:2px 0;position:relative;padding:4px 8px;border-radius:10px;transition:background-color .15s ease}
+/* Long-press must enter the app's own selection mode, not the browser's
+   native text-selection/callout menu — without this, a long-press on a
+   real touch device can race the JS timer against the OS text-selection
+   UI, which is what made selection feel unreliable/didn't-highlight. */
+.chat-row, .chat-bubble{ -webkit-touch-callout:none; -webkit-user-select:none; user-select:none; }
 .chat-row.me{justify-content:flex-end}
 .chat-bubble{max-width:82%;padding:8px 12px;border-radius:16px;font-size:13.5px;line-height:1.5;position:relative;cursor:pointer;transition:transform .1s}
 .chat-bubble.mine{background:#161616;color:#fff;border:1px solid rgba(255,255,255,.08);border-radius:16px 4px 16px 16px}
@@ -465,6 +471,7 @@
 .chat-gif{max-width:180px}
 .chat-date-sep{text-align:center;margin:14px 0 8px;font-size:10px;color:var(--text3);text-transform:uppercase;letter-spacing:.5px}
 .chat-jump-btn{position:absolute;right:14px;bottom:80px;width:38px;height:38px;border-radius:50%;background:rgba(20,20,20,.85);border:1px solid var(--border2);color:#fff;display:none;align-items:center;justify-content:center;cursor:pointer;z-index:6;font-size:14px}
+.chat-jump-btn svg{width:18px;height:18px}
 .chat-jump-btn.show{display:flex}
 .chat-jump-badge{position:absolute;top:-4px;right:-4px;background:var(--red);border-radius:10px;font-size:9px;padding:1px 5px;display:none}
 .chat-jump-badge:not(:empty){display:block}
@@ -472,7 +479,8 @@
 .chat-pinned-item{font-size:11px;color:var(--text2);cursor:pointer;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
 .chat-select-toolbar{display:none;align-items:center;gap:10px;padding:10px 14px;background:var(--red);color:#fff;position:sticky;top:0;z-index:6}
 .chat-select-toolbar.show{display:flex}
-.chat-select-toolbar button{background:rgba(255,255,255,.15);border:none;color:#fff;padding:6px 12px;border-radius:10px;cursor:pointer;font-size:12px}
+.chat-select-toolbar button{background:rgba(255,255,255,.15);border:none;color:#fff;padding:6px 12px;border-radius:10px;cursor:pointer;font-size:12px;display:inline-flex;align-items:center;gap:6px}
+.chat-select-toolbar button svg{width:16px;height:16px}
 .chat-select-toolbar .cnt{flex:1;font-size:13px;font-weight:600}
 .chat-search-bar{display:none;padding:8px 12px;border-bottom:1px solid var(--border);background:rgba(0,0,0,.4)}
 .chat-search-bar.show{display:block}
diff --git a/public/composer.css b/public/composer.css
index a1e9b2c..229fe7e 100644
--- a/public/composer.css
+++ b/public/composer.css
@@ -1 +1 @@
-.uc-composer{display:flex;align-items:center;gap:8px;padding:8px 12px;padding-bottom:max(18px,calc(14px + env(safe-area-inset-bottom)));padding-left:max(12px,env(safe-area-inset-left));padding-right:max(12px,env(safe-area-inset-right));background:0 0!important;border:none!important;border-top:none!important;border-radius:0;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important;flex-shrink:0;position:relative;z-index:6}.uc-composer--ai{background:0 0!important;border:none!important;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important}.ai-wrap,.chat-page-wrap,[class$="-page-wrap"]{background:0 0!important;border-top:none!important;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important}.uc-input-wrap{flex:1;min-width:0;display:flex;align-items:center;gap:2px;border:1px solid var(--border);border-radius:26px;background:var(--g2);padding:5px 7px}.uc-input-wrap:focus-within{border-color:var(--border);box-shadow:none}.uc-input{flex:1;min-width:0;width:100%;resize:none;box-sizing:border-box;max-height:130px;min-height:40px;border:none!important;border-radius:0;padding:9px 8px;font-size:15px;font-family:var(--ff-sans);line-height:1.4;background:0 0!important;color:var(--white)!important;outline:0;box-shadow:none!important;display:flex;align-items:center}.uc-input:focus{border:none!important;background:0 0!important;box-shadow:none!important;outline:0!important}.uc-input::placeholder{color:var(--text3)}.uc-input-wrap>.uc-input:first-child{padding-left:14px}.uc-input-wrap>.uc-input:last-child{padding-right:14px}.uc-composer--ai .uc-input{max-height:120px;font-size:14px}.uc-composer--ai .uc-input-wrap>.uc-input:first-child{padding-left:16px}.uc-composer--ai .uc-input-wrap>.uc-input:last-child{padding-right:16px}.uc-icon-btn{width:34px;height:34px;border-radius:50%;border:none!important;cursor:pointer;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:16px;background:0 0!important;box-shadow:none!important;color:var(--text2);transition:var(--t),transform .2s cubic-bezier(.34,1.56,.64,1)}.uc-icon-btn:hover{background:var(--g3)!important;color:var(--white)}.uc-icon-btn:active{transform:scale(.88)}.uc-icon-btn .plus-ico{font-size:20px;font-weight:300}.uc-icon-btn.is-open .plus-ico{transform:rotate(45deg);display:inline-block}.uc-camera-ico{width:19px;height:19px;display:block}.uc-composer button:active{box-shadow:none!important}.uc-btn{width:46px;height:46px;border-radius:50%;border:none;cursor:pointer;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:18px;line-height:1;background:var(--g2);color:var(--white);transition:var(--t),transform .15s cubic-bezier(.34, 1.56, .64, 1)}.uc-btn:hover{background:var(--g3)}.uc-btn:active{transform:scale(.9)}.uc-btn--send{background:linear-gradient(135deg,var(--accent),var(--accent-d));color:#fff;box-shadow:0 3px 12px var(--accent-glow);opacity:.55}.uc-btn--send.is-ready{opacity:1;transform:scale(1.02)}.uc-btn--send.is-sending{opacity:.6;pointer-events:none}.uc-btn--send.is-sending .ai-send-ico,.uc-btn--send.is-sending .plus-ico{animation:ucSpin .9s linear infinite;display:inline-block}@keyframes ucSpin{from{transform:rotate(0)}to{transform:rotate(360deg)}}.uc-btn--rec{background:var(--red);color:#fff;animation:ucRecPulse 1s infinite}@keyframes ucRecPulse{0%,100%{box-shadow:0 0 0 0 rgba(248,113,113,.5)}50%{box-shadow:0 0 0 8px rgba(248,113,113,0)}}.uc-btn--plus{transition:transform .2s cubic-bezier(.34,1.56,.64,1),background .2s}.uc-btn--plus:active{transform:rotate(45deg) scale(.9)}.uc-btn .plus-ico{font-size:20px;font-weight:300}.uc-btn--mic-send{position:relative;opacity:1}.uc-btn--mic-send .uc-mic-ico,.uc-btn--mic-send .uc-send-ico{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;transition:opacity .15s ease,transform .15s cubic-bezier(.34,1.56,.64,1)}.uc-btn--mic-send .uc-send-ico{opacity:0;transform:scale(.6) rotate(-20deg)}.uc-btn--mic-send .uc-mic-ico{opacity:1;transform:scale(1) rotate(0)}.uc-btn--mic-send.has-text .uc-send-ico{opacity:1;transform:scale(1) rotate(0)}.uc-btn--mic-send.has-text .uc-mic-ico{opacity:0;transform:scale(.6) rotate(20deg)}.uc-composer--ai .uc-btn{width:42px;height:42px;font-size:16px}.uc-rec-timer{font-size:11px;color:var(--red);font-weight:700;align-self:center;flex-shrink:0}.uc-chips{display:flex;flex-wrap:wrap;align-items:stretch;align-content:flex-start;gap:7px;padding:6px 14px 10px}.uc-chip{display:inline-flex;align-items:center;padding:7px 14px;border-radius:20px;border:1px solid var(--border);background:var(--g1);font-size:11px;font-weight:500;line-height:1.3;color:var(--text2);cursor:pointer;user-select:none;white-space:nowrap;transition:var(--t)}.uc-chip:hover{background:var(--g2);color:var(--white);border-color:var(--border2)}.uc-chip:active{transform:scale(.96)}@media (max-width:700px){.uc-chips{padding:4px 10px 8px;gap:6px}.uc-chip{font-size:10.5px;padding:6px 11px}}
\ No newline at end of file
+.uc-composer{display:flex;align-items:center;gap:8px;padding:8px 12px;padding-bottom:max(18px,calc(14px + env(safe-area-inset-bottom)));padding-left:max(12px,env(safe-area-inset-left));padding-right:max(12px,env(safe-area-inset-right));background:0 0!important;border:none!important;border-top:none!important;border-radius:0;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important;flex-shrink:0;position:relative;z-index:6}.uc-composer--ai{background:0 0!important;border:none!important;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important}.ai-wrap,.chat-page-wrap,[class$="-page-wrap"]{background:0 0!important;border-top:none!important;box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important}.uc-input-wrap{flex:1;min-width:0;display:flex;align-items:center;gap:2px;border:1px solid var(--border);border-radius:26px;background:var(--g2);padding:5px 7px}.uc-input-wrap:focus-within{border-color:var(--border);box-shadow:none}.uc-input{flex:1;min-width:0;width:100%;resize:none;box-sizing:border-box;max-height:130px;min-height:40px;border:none!important;border-radius:0;padding:9px 8px;font-size:15px;font-family:var(--ff-sans);line-height:1.4;background:0 0!important;color:var(--white)!important;outline:0;box-shadow:none!important;display:flex;align-items:center}.uc-input:focus{border:none!important;background:0 0!important;box-shadow:none!important;outline:0!important}.uc-input::placeholder{color:var(--text3)}.uc-input-wrap>.uc-input:first-child{padding-left:14px}.uc-input-wrap>.uc-input:last-child{padding-right:14px}.uc-composer--ai .uc-input{max-height:120px;font-size:14px}.uc-composer--ai .uc-input-wrap>.uc-input:first-child{padding-left:16px}.uc-composer--ai .uc-input-wrap>.uc-input:last-child{padding-right:16px}.uc-icon-btn{width:34px;height:34px;border-radius:50%;border:none!important;cursor:pointer;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:16px;background:0 0!important;box-shadow:none!important;color:var(--text2);transition:var(--t),transform .2s cubic-bezier(.34,1.56,.64,1)}.uc-icon-btn:hover{background:var(--g3)!important;color:var(--white)}.uc-icon-btn:active{transform:scale(.88)}.uc-icon-btn .plus-ico{font-size:20px;font-weight:300}.uc-icon-btn.is-open .plus-ico{transform:rotate(45deg);display:inline-block}.uc-camera-ico{width:19px;height:19px;display:block}.uc-composer button:active{box-shadow:none!important}.uc-btn{width:46px;height:46px;border-radius:50%;border:none;cursor:pointer;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:18px;line-height:1;background:var(--g2);color:var(--white);transition:var(--t),transform .15s cubic-bezier(.34, 1.56, .64, 1)}.uc-btn svg{width:19px;height:19px}.uc-btn:hover{background:var(--g3)}.uc-btn:active{transform:scale(.9)}.uc-btn--send{background:linear-gradient(135deg,var(--accent),var(--accent-d));color:#fff;box-shadow:0 3px 12px var(--accent-glow);opacity:.55}.uc-btn--send.is-ready{opacity:1;transform:scale(1.02)}.uc-btn--send.is-sending{opacity:.6;pointer-events:none}.uc-btn--send.is-sending .ai-send-ico,.uc-btn--send.is-sending .plus-ico{animation:ucSpin .9s linear infinite;display:inline-block}@keyframes ucSpin{from{transform:rotate(0)}to{transform:rotate(360deg)}}.uc-btn--rec{background:var(--red);color:#fff;animation:ucRecPulse 1s infinite}@keyframes ucRecPulse{0%,100%{box-shadow:0 0 0 0 rgba(248,113,113,.5)}50%{box-shadow:0 0 0 8px rgba(248,113,113,0)}}.uc-btn--plus{transition:transform .2s cubic-bezier(.34,1.56,.64,1),background .2s}.uc-btn--plus:active{transform:rotate(45deg) scale(.9)}.uc-btn .plus-ico{font-size:20px;font-weight:300}.uc-btn--mic-send{position:relative;opacity:1}.uc-btn--mic-send .uc-mic-ico,.uc-btn--mic-send .uc-send-ico{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;transition:opacity .15s ease,transform .15s cubic-bezier(.34,1.56,.64,1)}.uc-btn--mic-send .uc-send-ico{opacity:0;transform:scale(.6) rotate(-20deg)}.uc-btn--mic-send .uc-mic-ico{opacity:1;transform:scale(1) rotate(0)}.uc-btn--mic-send.has-text .uc-send-ico{opacity:1;transform:scale(1) rotate(0)}.uc-btn--mic-send.has-text .uc-mic-ico{opacity:0;transform:scale(.6) rotate(20deg)}.uc-composer--ai .uc-btn{width:42px;height:42px;font-size:16px}.uc-rec-timer{font-size:11px;color:var(--red);font-weight:700;align-self:center;flex-shrink:0}.uc-chips{display:flex;flex-wrap:wrap;align-items:stretch;align-content:flex-start;gap:7px;padding:6px 14px 10px}.uc-chip{display:inline-flex;align-items:center;padding:7px 14px;border-radius:20px;border:1px solid var(--border);background:var(--g1);font-size:11px;font-weight:500;line-height:1.3;color:var(--text2);cursor:pointer;user-select:none;white-space:nowrap;transition:var(--t)}.uc-chip:hover{background:var(--g2);color:var(--white);border-color:var(--border2)}.uc-chip:active{transform:scale(.96)}@media (max-width:700px){.uc-chips{padding:4px 10px 8px;gap:6px}.uc-chip{font-size:10.5px;padding:6px 11px}}
\ No newline at end of file
diff --git a/public/index.html b/public/index.html
index d0c0e9a..68e955f 100644
--- a/public/index.html
+++ b/public/index.html
@@ -913,6 +913,11 @@
       border: 1px solid var(--border);
     }
 
+    .ic-btn svg {
+      width: 17px;
+      height: 17px;
+    }
+
     .ic-btn:hover {
       background: var(--g3);
       transform: scale(1.07)
@@ -2234,7 +2239,15 @@
       border: none;
       padding: 2px 6px;
       border-radius: 6px;
-      transition: var(--t)
+      transition: var(--t);
+      display: inline-flex;
+      align-items: center;
+      justify-content: center;
+    }
+
+    .modal-close svg {
+      width: 18px;
+      height: 18px;
     }
 
     .modal-close:hover {
@@ -2390,6 +2403,12 @@
       -webkit-user-drag: none
     }
 
+    .mem-viewer-stage img {
+      transform-origin: center center;
+      will-change: transform;
+      touch-action: none;
+    }
+
     .mem-viewer-nav {
       position: absolute;
       top: 50%;
@@ -2463,10 +2482,22 @@
       line-height: 1
     }
 
+    .mem-viewer-action .ico svg {
+      width: 19px;
+      height: 19px;
+      display: block;
+      stroke-width: 2px;
+    }
+
     .mem-viewer-action.fav.active .ico {
       color: #ff5c72
     }
 
+    .mem-viewer-action.fav.active .ico svg {
+      fill: #ff5c72;
+      stroke: #ff5c72;
+    }
+
     /* Bottom sheet used by both "More" (actions) and "Details" (metadata) */
     .mem-sheet-overlay {
       position: fixed;
@@ -3942,6 +3973,13 @@
   transform: translateY(-50%);
   font-size: 18px;
   pointer-events: none;
+  color: var(--text3);
+  display: flex;
+}
+
+.search-ico svg {
+  width: 17px;
+  height: 17px;
 }
 
 .search-cancel {
@@ -4858,16 +4896,16 @@ select option { background: #0D0D0D; color: #fff; }
     </div>
     <div class="mem-viewer-bar" id="memViewerBar">
       <button type="button" class="mem-viewer-action fav" id="memViewerFavBtn" onclick="memViewerToggleFav()">
-        <span class="ico" id="memViewerFavIcon">🤍</span><span>Favorite</span>
+        <span class="ico" id="memViewerFavIcon"><i data-lucide="heart"></i></span><span>Favorite</span>
       </button>
       <button type="button" class="mem-viewer-action" onclick="memViewerShare()">
-        <span class="ico">↗️</span><span>Share</span>
+        <span class="ico"><i data-lucide="share-2"></i></span><span>Share</span>
       </button>
       <button type="button" class="mem-viewer-action" onclick="memViewerDeleteCurrent()">
-        <span class="ico">🗑️</span><span>Delete</span>
+        <span class="ico"><i data-lucide="trash-2"></i></span><span>Delete</span>
       </button>
       <button type="button" class="mem-viewer-action" onclick="memViewerOpenMore()">
-        <span class="ico">⋮</span><span>More</span>
+        <span class="ico"><i data-lucide="more-vertical"></i></span><span>More</span>
       </button>
     </div>
   </div>
@@ -4960,7 +4998,7 @@ select option { background: #0D0D0D; color: #fff; }
 <div class="search-overlay" id="searchOverlay">
   <div class="search-header">
     <div class="search-input-wrap">
-      <span class="search-ico">🔍</span>
+      <span class="search-ico"><i data-lucide="search"></i></span>
       <input type="text" id="searchInput" placeholder="Search everything..." 
         oninput="doSearch(this.value)" autocomplete="off">
     </div>
@@ -5092,7 +5130,7 @@ select option { background: #0D0D0D; color: #fff; }
           <div id="hbTopbarPill" class="hb-topbar-pill" onclick="HeartbeatManager.openStats()" title="Heartbeat Stats"></div>
         </div>
         <div class="tb-actions">
-          <button class="ic-btn" onclick="openSearch()" title="Search" style="font-size:16px">🔍</button>
+          <button class="ic-btn" onclick="openSearch()" title="Search"><i data-lucide="search"></i></button>
           <button class="btn btn-glass btn-sm" onclick="syncNow()" id="syncBtn">☁️ Sync</button>
         </div>
       </div>
@@ -5246,10 +5284,10 @@ select option { background: #0D0D0D; color: #fff; }
 <div class="page" id="page-chat">
   <div class="chat-page-wrap">
     <div class="chat-select-toolbar" id="chatSelectToolbar">
-      <button onclick="Chat.exitSelectMode()">✕</button>
+      <button onclick="Chat.exitSelectMode()" aria-label="Exit selection"><i data-lucide="x"></i></button>
       <div class="cnt" id="chatSelectCount">1 selected</div>
-      <button id="chatSelectMoreBtn" onclick="Chat.openSelectedMsgMenu(event)" style="display:none">⋮</button>
-      <button id="chatSelectTrashBtn" onclick="Chat.deleteSelected()">🗑️ Delete</button>
+      <button id="chatSelectMoreBtn" onclick="Chat.openSelectedMsgMenu(event)" style="display:none" aria-label="More"><i data-lucide="more-vertical"></i></button>
+      <button id="chatSelectTrashBtn" onclick="Chat.deleteSelected()"><i data-lucide="trash-2"></i> Delete</button>
     </div>
 
     <div class="chat-header">
@@ -5259,10 +5297,10 @@ select option { background: #0D0D0D; color: #fff; }
         <div class="chat-header-status" id="chatHeaderStatus">offline</div>
       </div>
       <div class="chat-header-actions">
-  <button class="ic-btn" id="chatVoiceCallBtn" style="width:34px;height:34px;font-size:14px" title="Voice Call">📞</button>
-  <button class="ic-btn" id="chatVideoCallBtn" style="width:34px;height:34px;font-size:14px" title="Video Call">📹</button>
-  <button class="ic-btn" style="width:34px;height:34px;font-size:14px" onclick="Chat.openStarred()" title="Starred">⭐</button>
-  <button class="ic-btn" style="width:34px;height:34px;font-size:14px" onclick="Chat.openSearch()" title="Search">🔍</button>
+  <button class="ic-btn" id="chatVoiceCallBtn" style="width:34px;height:34px" title="Voice Call"><i data-lucide="phone"></i></button>
+  <button class="ic-btn" id="chatVideoCallBtn" style="width:34px;height:34px" title="Video Call"><i data-lucide="video"></i></button>
+  <button class="ic-btn" style="width:34px;height:34px" onclick="Chat.openStarred()" title="Starred"><i data-lucide="star"></i></button>
+  <button class="ic-btn" style="width:34px;height:34px" onclick="Chat.openSearch()" title="Search"><i data-lucide="search"></i></button>
 </div>
     </div>
 <script>
@@ -5312,8 +5350,8 @@ select option { background: #0D0D0D; color: #fff; }
     <div class="chat-pinned-bar" id="chatPinnedBar"></div>
 
     <div class="chat-msgs" id="chatMsgs" onscroll="Chat.onChatScroll()"></div>
-    <button class="chat-jump-btn" id="chatJumpBtn" onclick="Chat.scrollToBottom(true)">
-      ⬇<span class="chat-jump-badge" data-chat-badge></span>
+    <button class="chat-jump-btn" id="chatJumpBtn" onclick="Chat.scrollToBottom(true)" aria-label="Jump to latest">
+      <i data-lucide="chevron-down"></i><span class="chat-jump-badge" data-chat-badge></span>
     </button>
 
     <div class="chat-composer-banner" id="chatComposerBanner"></div>
@@ -5335,8 +5373,8 @@ select option { background: #0D0D0D; color: #fff; }
         <input type="file" id="chatCameraInputQuick" accept="image/*" capture="environment" style="display:none" onchange="Chat.onImagePick(this)">
       </div>
       <span class="uc-rec-timer" id="chatRecTimer" style="display:none">00:00</span>
-      <button class="uc-btn" id="chatCancelRecBtn" style="display:none" onclick="Chat.cancelRecording()" title="Discard">🗑️</button>
-      <button class="uc-btn uc-btn--rec" id="chatStopRecBtn" style="display:none" onclick="Chat.toggleRecord()" title="Send voice message">✔</button>
+      <button class="uc-btn" id="chatCancelRecBtn" style="display:none" onclick="Chat.cancelRecording()" title="Discard"><i data-lucide="trash-2"></i></button>
+      <button class="uc-btn uc-btn--rec" id="chatStopRecBtn" style="display:none" onclick="Chat.toggleRecord()" title="Send voice message"><i data-lucide="check"></i></button>
       <button class="uc-btn uc-btn--send uc-btn--mic-send" id="chatSendBtn" onclick="chatMicSendClick()" title="Voice message">
         <span class="uc-mic-ico">🎙️</span>
         <span class="uc-send-ico">➤</span>
@@ -6067,11 +6105,11 @@ select option { background: #0D0D0D; color: #fff; }
         persisted in S like everything else, so they sync via scheduleSave(). -->
    <div style="position:relative;margin-bottom:10px">
      <div style="display:flex;align-items:center;gap:6px;background:var(--g1);border:1px solid var(--border);border-radius:var(--r);padding:9px 12px">
-       <span style="opacity:.55;font-size:13px">🔍</span>
+       <span style="opacity:.55;display:flex"><i data-lucide="search" style="width:14px;height:14px"></i></span>
        <input type="text" id="lmGmSearchInput" placeholder="Search Bengaluru, Vijayawada, restaurants, hospitals…"
          oninput="LiveMap.gmSearchInput(this.value)" onfocus="LiveMap.gmSearchFocus()"
          style="flex:1;min-width:0;background:transparent;border:none;outline:none;color:var(--white);font-size:12px">
-       <span id="lmGmSearchClear" style="display:none;cursor:pointer;opacity:.55;font-size:13px" onclick="LiveMap.gmSearchClear()">✕</span>
+       <span id="lmGmSearchClear" style="display:none;cursor:pointer;opacity:.55" onclick="LiveMap.gmSearchClear()"><i data-lucide="x" style="width:14px;height:14px;display:block"></i></span>
      </div>
      <div id="lmGmSearchResults" class="lm-search-results" style="position:absolute;left:0;right:0;top:100%;z-index:500"></div>
    </div>
@@ -6148,7 +6186,7 @@ select option { background: #0D0D0D; color: #fff; }
 <!-- ADD PLACE MODAL -->
 <div class="modal-bg" id="lmPlaceModal">
   <div class="modal">
-    <div class="modal-title">📌 Add Important Place<button class="modal-close" onclick="closeM('lmPlaceModal')">✕</button></div>
+    <div class="modal-title">📌 Add Important Place<button class="modal-close" onclick="closeM('lmPlaceModal')"><i data-lucide="x"></i></button></div>
     <div class="field"><label>Category</label>
       <select id="lmPlaceCat" onchange="LiveMap.onCatChange()">
         <option value="Home">🏠 Home</option>
@@ -6187,8 +6225,8 @@ select option { background: #0D0D0D; color: #fff; }
 <div class="modal-bg" id="lmRouteModal">
   <div class="modal">
     <div class="modal-title">🛣️ Daily Route
-      <button class="modal-close" style="right:44px" title="Route History privacy settings" onclick="LiveMap.toggleRouteSettingsPanel()">⚙️</button>
-      <button class="modal-close" onclick="closeM('lmRouteModal')">✕</button>
+      <button class="modal-close" style="right:44px" title="Route History privacy settings" onclick="LiveMap.toggleRouteSettingsPanel()"><i data-lucide="settings"></i></button>
+      <button class="modal-close" onclick="closeM('lmRouteModal')"><i data-lucide="x"></i></button>
     </div>
 
     <!-- Phase 4: privacy — off by default, each person controls only their own recording -->
@@ -6223,7 +6261,7 @@ select option { background: #0D0D0D; color: #fff; }
 <!-- PHASE 2: STREET VIEW MODAL (Mapillary, graceful Google Maps fallback) -->
 <div class="modal-bg" id="lmStreetViewModal">
   <div class="modal">
-    <div class="modal-title">👁 Street View<button class="modal-close" onclick="closeM('lmStreetViewModal')">✕</button></div>
+    <div class="modal-title">👁 Street View<button class="modal-close" onclick="closeM('lmStreetViewModal')"><i data-lucide="x"></i></button></div>
     <div id="lmStreetViewBody"><div class="empty">Loading…</div></div>
   </div>
 </div>
@@ -6644,7 +6682,7 @@ select option { background: #0D0D0D; color: #fff; }
       <!-- ════════ MODALS ════════ -->
       <div class="modal-bg" id="bucketModal">
         <div class="modal">
-          <div class="modal-title">🌟 Add a Dream<button class="modal-close" onclick="closeM('bucketModal')">✕</button>
+          <div class="modal-title">🌟 Add a Dream<button class="modal-close" onclick="closeM('bucketModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>Dream / Goal</label><input type="text" id="bucketTitle"
               placeholder="e.g. Travel to Paris together"></div>
@@ -6670,7 +6708,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="dreamModal">
         <div class="modal">
           <div class="modal-title">🏡 Add Dream Idea<button class="modal-close"
-              onclick="closeM('dreamModal')">✕</button></div>
+              onclick="closeM('dreamModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Category</label><select id="dreamCat">
               <option value="House">🏠 House</option>
               <option value="Furniture">🛋️ Furniture</option>
@@ -6690,7 +6728,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="surpriseModal">
         <div class="modal">
           <div class="modal-title">🎁 New Surprise<button class="modal-close"
-              onclick="closeM('surpriseModal')">✕</button></div>
+              onclick="closeM('surpriseModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Type</label><select id="surType">
               <option value="🎁">🎁 Gift idea</option>
               <option value="💌">💌 Letter</option>
@@ -6710,7 +6748,7 @@ select option { background: #0D0D0D; color: #fff; }
       </div>
       <div class="modal-bg" id="placeModal">
         <div class="modal">
-          <div class="modal-title">📌 Add Place<button class="modal-close" onclick="closeM('placeModal')">✕</button>
+          <div class="modal-title">📌 Add Place<button class="modal-close" onclick="closeM('placeModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>Label</label><select id="placeLabel">
               <option value="Home">🏠 Home</option>
@@ -6730,7 +6768,7 @@ select option { background: #0D0D0D; color: #fff; }
       </div>
       <div class="modal-bg" id="eventModal">
         <div class="modal">
-          <div class="modal-title">📅 Add Event<button class="modal-close" onclick="closeM('eventModal')">✕</button>
+          <div class="modal-title">📅 Add Event<button class="modal-close" onclick="closeM('eventModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>Event Name</label><input type="text" id="evTitle"
               placeholder="e.g. Anniversary Dinner"></div>
@@ -6761,7 +6799,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="moneyModal">
         <div class="modal">
           <div class="modal-title">💰 Add Transaction<button class="modal-close"
-              onclick="closeM('moneyModal')">✕</button></div>
+              onclick="closeM('moneyModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Type</label><select id="moneyType">
               <option value="expense">💸 Expense</option>
               <option value="income">💰 Income</option>
@@ -6781,7 +6819,7 @@ select option { background: #0D0D0D; color: #fff; }
 
       <div class="modal-bg" id="habitModal">
         <div class="modal">
-          <div class="modal-title">✅ Add Habit<button class="modal-close" onclick="closeM('habitModal')">✕</button>
+          <div class="modal-title">✅ Add Habit<button class="modal-close" onclick="closeM('habitModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>Habit Name</label><input type="text" id="habitName" placeholder="e.g. Morning walk">
           </div>
@@ -6798,7 +6836,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="milestoneModal">
         <div class="modal">
           <div class="modal-title">💫 Add Milestone<button class="modal-close"
-              onclick="closeM('milestoneModal')">✕</button></div>
+              onclick="closeM('milestoneModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Milestone</label><input type="text" id="msTitle"
               placeholder="e.g. First trip together"></div>
           <div class="field"><label>Type</label><select id="msType">
@@ -6819,7 +6857,7 @@ select option { background: #0D0D0D; color: #fff; }
 
       <div class="modal-bg" id="periodModal">
         <div class="modal">
-          <div class="modal-title">🩷 Log Period<button class="modal-close" onclick="closeM('periodModal')">✕</button>
+          <div class="modal-title">🩷 Log Period<button class="modal-close" onclick="closeM('periodModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>Period Start Date</label><input type="date" id="periodStart"></div>
           <div class="row2">
@@ -6836,7 +6874,7 @@ select option { background: #0D0D0D; color: #fff; }
 
       <div class="modal-bg" id="fightModal">
         <div class="modal">
-          <div class="modal-title">⚡ Log a Fight<button class="modal-close" onclick="closeM('fightModal')">✕</button>
+          <div class="modal-title">⚡ Log a Fight<button class="modal-close" onclick="closeM('fightModal')"><i data-lucide="x"></i></button>
           </div>
           <div class="field"><label>What was the fight about?</label><input type="text" id="fightTitle"
               placeholder="e.g. Communication, plans, jealousy..."></div>
@@ -6861,7 +6899,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="studySubjectModal">
         <div class="modal">
           <div class="modal-title">📚 Add Subject<button class="modal-close"
-              onclick="closeM('studySubjectModal')">✕</button></div>
+              onclick="closeM('studySubjectModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Subject Name</label><input type="text" id="subjName" placeholder="e.g. Mathematics">
           </div>
           <div class="row2">
@@ -6878,7 +6916,7 @@ select option { background: #0D0D0D; color: #fff; }
       <div class="modal-bg" id="studySlotModal">
         <div class="modal">
           <div class="modal-title">🗓️ Add Timetable Slot<button class="modal-close"
-              onclick="closeM('studySlotModal')">✕</button></div>
+              onclick="closeM('studySlotModal')"><i data-lucide="x"></i></button></div>
           <div class="field"><label>Day</label><select id="slotDay">
               <option>Monday</option>
               <option>Tuesday</option>
@@ -7725,7 +7763,66 @@ function setJustPairedAt(ts) {
           }
           closeMobSidebar();
         }
+        // ─── Shared back-navigation priority chain ──────────────
+        // Single source of truth for "what does Back close first" —
+        // used by BOTH the Android hardware back button (Capacitor)
+        // AND the browser/PWA popstate handler below, so header back
+        // buttons, hardware back, and browser back gestures all behave
+        // consistently instead of being two separate systems.
+        // Returns true if it closed/consumed something (caller should
+        // stop there), false if normal page-level back should proceed.
+        function handleBackPriorityLayers() {
+          // 0) The fullscreen photo/video viewer takes top priority.
+          const openViewer = document.getElementById('imgViewer');
+          if (openViewer && openViewer.classList.contains('open')) { closeImgViewer(); return true; }
+          // 0.1) An open safe-delete confirmation sheet takes priority.
+          const openConfirm = document.querySelector('.sd-overlay.show');
+          if (openConfirm) { openConfirm.classList.remove('show'); return true; }
+          // 0.2) Memories gallery viewer's own overlays take priority over
+          //      closing the viewer itself — details sheet, then more sheet.
+          const openDetails = document.getElementById('memDetailsSheet');
+          if (openDetails && openDetails.classList.contains('show')) { memViewerCloseDetails(); return true; }
+          const openMore = document.getElementById('memMoreSheet');
+          if (openMore && openMore.classList.contains('show')) { memViewerCloseMore(); return true; }
+          // 0.3) Memories gallery viewer open — close it, stay on Our Memories.
+          const openMemViewerEl = document.getElementById('memViewer');
+          if (openMemViewerEl && openMemViewerEl.classList.contains('open')) { closeMemViewer(); return true; }
+          // 0.5) Memories selection mode active — exit selection, don't navigate.
+          if (typeof memSelectMode !== 'undefined' && memSelectMode) { memExitSelectMode(); return true; }
+          // 0.55) Calendar selection mode active — exit selection, don't navigate.
+          if (typeof calSelectMode !== 'undefined' && calSelectMode) { calExitSelectMode(); return true; }
+          // 0.56) Bucket List selection mode active — exit selection, don't navigate.
+          if (typeof bucketSelectMode !== 'undefined' && bucketSelectMode) { bucketExitSelectMode(); return true; }
+          // 0.57) Chat's own message action-menu / selection mode take priority.
+          if (window.Chat && typeof window.Chat.closeMsgMenuIfOpen === 'function' && window.Chat.closeMsgMenuIfOpen()) { return true; }
+          if (window.Chat && typeof window.Chat.isSelecting === 'function' && window.Chat.isSelecting()) { window.Chat.exitSelectMode(); return true; }
+          // 0.6) Dream Goals lives in its own same-origin iframe with its own
+          //      safe-delete confirm sheet + selection mode — check those
+          //      before falling through to page navigation.
+          if (document.getElementById('page-dreamgoals')?.classList.contains('active')) {
+            const dgWin = document.getElementById('dreamgoalsFrame')?.contentWindow;
+            try {
+              const dgConfirm = dgWin?.document.querySelector('.sd-overlay.show');
+              if (dgConfirm) { dgConfirm.classList.remove('show'); return true; }
+              if (dgWin?.dgSelectMode) { dgWin.dgExitSelectMode(); return true; }
+            } catch (e) {}
+          }
+          // 1) An open modal takes priority — just close it.
+          const openModal = document.querySelector('.modal-bg.open');
+          if (openModal) { openModal.classList.remove('open'); return true; }
+          return false;
+        }
         window.addEventListener('popstate', (e) => {
+  // Browser/PWA back gesture and the header/hardware back button must
+  // agree on what closes first. If a modal/viewer/selection-mode layer
+  // is open, close only that layer and restore the history entry the
+  // browser just consumed (so the page underneath doesn't also change
+  // and back still has a normal page-level entry to use next time).
+  if (handleBackPriorityLayers()) {
+    const cur = (window.PageManager && window.PageManager.currentPage) || (e.state && e.state.page) || 'dashboard';
+    history.pushState({ page: cur }, '', '#' + cur);
+    return;
+  }
   if (e.state && e.state.page) {
     goto(e.state.page, false); // false = don't push a new history entry
   } else {
@@ -7760,64 +7857,22 @@ function setJustPairedAt(ts) {
           const CapApp = window.Capacitor.Plugins?.App;
           if (CapApp && typeof CapApp.addListener === 'function') {
             CapApp.addListener('backButton', () => {
-              // 0) The fullscreen photo/video viewer takes top priority —
-              //    close it only. Previously this wasn't checked at all, so
-              //    back (or Android's edge-swipe-back gesture, which fires
-              //    the same 'backButton' event) fell straight through to
-              //    page navigation or minimizeApp() while the viewer stayed
-              //    stuck on top — which looked like the app itself closing.
-              const openViewer = document.getElementById('imgViewer');
-              if (openViewer && openViewer.classList.contains('open')) { closeImgViewer(); return; }
-              // 0.1) An open safe-delete confirmation sheet takes priority — close it only.
-              const openConfirm = document.querySelector('.sd-overlay.show');
-              if (openConfirm) { openConfirm.classList.remove('show'); return; }
-              // 0.2) Memories gallery viewer's own overlays take priority over
-              //      closing the viewer itself — details sheet, then more sheet.
-              const openDetails = document.getElementById('memDetailsSheet');
-              if (openDetails && openDetails.classList.contains('show')) { memViewerCloseDetails(); return; }
-              const openMore = document.getElementById('memMoreSheet');
-              if (openMore && openMore.classList.contains('show')) { memViewerCloseMore(); return; }
-              // 0.3) Memories gallery viewer open — close it, stay on Our Memories.
-              const openMemViewerEl = document.getElementById('memViewer');
-              if (openMemViewerEl && openMemViewerEl.classList.contains('open')) { closeMemViewer(); return; }
-              // 0.5) Memories selection mode active — exit selection, don't navigate.
-              if (typeof memSelectMode !== 'undefined' && memSelectMode) { memExitSelectMode(); return; }
-              // 0.55) Calendar selection mode active — exit selection, don't navigate.
-              if (typeof calSelectMode !== 'undefined' && calSelectMode) { calExitSelectMode(); return; }
-              // 0.56) Bucket List selection mode active — exit selection, don't navigate.
-              if (typeof bucketSelectMode !== 'undefined' && bucketSelectMode) { bucketExitSelectMode(); return; }
-              // 0.57) Chat's own message action-menu / selection mode take priority.
-              if (window.Chat && typeof window.Chat.closeMsgMenuIfOpen === 'function' && window.Chat.closeMsgMenuIfOpen()) { return; }
-              if (window.Chat && typeof window.Chat.isSelecting === 'function' && window.Chat.isSelecting()) { window.Chat.exitSelectMode(); return; }
-              // 0.6) Dream Goals lives in its own same-origin iframe with its own
-              //      safe-delete confirm sheet + selection mode — check those
-              //      before falling through to page navigation.
-              if (document.getElementById('page-dreamgoals')?.classList.contains('active')) {
-                const dgWin = document.getElementById('dreamgoalsFrame')?.contentWindow;
-                try {
-                  const dgConfirm = dgWin?.document.querySelector('.sd-overlay.show');
-                  if (dgConfirm) { dgConfirm.classList.remove('show'); return; }
-                  if (dgWin?.dgSelectMode) { dgWin.dgExitSelectMode(); return; }
-                } catch (e) {}
-              }
-              // 1) An open modal takes priority — just close it.
-              const openModal = document.querySelector('.modal-bg.open');
-              if (openModal) { openModal.classList.remove('open'); return; }
-              // 2) Not on the dashboard — step back ONE entry through the
-              //    real navigation stack (same stack goto() has been
-              //    pushing to all along), not straight to Dashboard.
-              //    history.back() triggers the existing popstate handler,
-              //    which reads e.state.page and restores exactly that page.
+              // Same layered priority chain the browser/PWA popstate
+              // handler uses — see handleBackPriorityLayers() above.
+              if (handleBackPriorityLayers()) return;
+              // Not on the dashboard — step back ONE entry through the
+              // real navigation stack (same stack goto() has been
+              // pushing to all along), not straight to Dashboard.
+              // history.back() triggers the existing popstate handler,
+              // which reads e.state.page and restores exactly that page.
               const onDashboard = document.getElementById('page-dashboard')?.classList.contains('active');
               if (!onDashboard) { history.back(); return; }
-              // 3) Already at the dashboard (top level) — minimize
-              //    instead of exiting or falling through to landing.html.
+              // Already at the dashboard (top level) — minimize
+              // instead of exiting or falling through to landing.html.
               CapApp.minimizeApp?.();
             });
           }
         }
-        function openM(id) { document.getElementById(id).classList.add('open'); }
-
         function openM(id) { document.getElementById(id).classList.add('open'); }
         function closeM(id) { document.getElementById(id).classList.remove('open'); }
         function esc(s) { return String(s || '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c])); }
@@ -8336,7 +8391,7 @@ const p = await Notification.requestPermission();
           try {
             const media = await uploadMediaFile(file, file.name, 'video');
             savePhoto(media.url, file.name, 'video', media.path);
-          } catch (e) { toast('Upload failed for ' + file.name); }
+          } catch (e) { toast(e.message || ('Upload failed for ' + file.name)); }
         }
         function savePhoto(url, name, type, path) { S.photos.push({ id: Date.now() + '_' + Math.random().toString(36).slice(2, 7), url, name, type: type || 'image', date: todayStr(), path: path || null }); renderPhotos(); scheduleSave(); }
         function renderPhotos() {
@@ -8523,6 +8578,7 @@ const p = await Notification.requestPermission();
           const v = document.getElementById('memViewerVideo');
           if (v) { v.pause(); v.removeAttribute('src'); v.load(); }
           memViewerIndex = -1;
+          if (window.memZoom) memZoom.reset(false); // don't leave a stale transform for the next open
         }
 
         function memViewerCurrent() {
@@ -8533,6 +8589,7 @@ const p = await Notification.requestPermission();
         function memViewerRender() {
           const p = memViewerCurrent();
           if (!p) { closeMemViewer(); return; }
+          if (window.memZoom) memZoom.reset(false); // never carry a zoom/pan transform onto a new image
           const list = memViewerList();
           const img = document.getElementById('memViewerImg');
           const vid = document.getElementById('memViewerVideo');
@@ -8548,8 +8605,6 @@ const p = await Notification.requestPermission();
           document.getElementById('memViewerCounter').textContent = (memViewerIndex + 1) + ' / ' + list.length;
           document.getElementById('memViewerPrevBtn').style.visibility = memViewerIndex > 0 ? 'visible' : 'hidden';
           document.getElementById('memViewerNextBtn').style.visibility = memViewerIndex < list.length - 1 ? 'visible' : 'hidden';
-          const favIcon = document.getElementById('memViewerFavIcon');
-          favIcon.textContent = p.favorite ? '❤️' : '🤍';
           document.getElementById('memViewerFavBtn').classList.toggle('active', !!p.favorite);
         }
 
@@ -8627,23 +8682,151 @@ const p = await Notification.requestPermission();
         }
         function memViewerCloseDetails() { document.getElementById('memDetailsSheet').classList.remove('show'); }
 
+        // ── Pinch-to-zoom / pan / double-tap for the memory image viewer ──
+        // Real gesture handling (pointer-events based, multi-touch aware) —
+        // not a fake `transform:scale()` on tap. Only applies to the <img>;
+        // videos keep their native controls untouched. Exposed as
+        // window.memZoom so memViewerRender()/closeMemViewer() can reset it.
+        window.memZoom = (function () {
+          const stage = document.getElementById('memViewerStage');
+          const img = document.getElementById('memViewerImg');
+          const MIN = 1, MAX = 4, DOUBLE_TAP_ZOOM = 2.5, DOUBLE_TAP_MS = 300, TAP_MOVE_TOL = 10;
+          let scale = 1, tx = 0, ty = 0;
+          const pointers = new Map();
+          let pinchStartDist = 0, pinchStartScale = 1;
+          let panStart = null;
+          let lastTapTime = 0, lastTapX = 0, lastTapY = 0;
+          let downX = 0, downY = 0, moved = false;
+
+          function apply(animate) {
+            img.style.transition = animate ? 'transform .25s cubic-bezier(.22,.8,.25,1)' : 'none';
+            img.style.transform = `translate(${tx}px,${ty}px) scale(${scale})`;
+          }
+
+          function clampPan() {
+            if (scale <= 1) { tx = 0; ty = 0; return; }
+            const rect = stage.getBoundingClientRect();
+            const iw = img.clientWidth * scale, ih = img.clientHeight * scale;
+            const maxX = Math.max(0, (iw - rect.width) / 2);
+            const maxY = Math.max(0, (ih - rect.height) / 2);
+            tx = Math.min(maxX, Math.max(-maxX, tx));
+            ty = Math.min(maxY, Math.max(-maxY, ty));
+          }
+
+          function reset(animate) {
+            scale = 1; tx = 0; ty = 0;
+            pointers.clear(); pinchStartDist = 0; panStart = null;
+            apply(animate === false ? false : true);
+          }
+
+          function dist(a, b) { return Math.hypot(a.x - b.x, a.y - b.y); }
+
+          function isImageActive() { return img.style.display !== 'none'; }
+
+          function onDown(e) {
+            if (!isImageActive() || e.target.closest('.mem-viewer-nav')) return;
+            pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
+            downX = e.clientX; downY = e.clientY; moved = false;
+            if (pointers.size === 2) {
+              const [a, b] = [...pointers.values()];
+              pinchStartDist = dist(a, b);
+              pinchStartScale = scale;
+              panStart = null;
+            } else if (pointers.size === 1 && scale > 1) {
+              panStart = { x: e.clientX, y: e.clientY, tx, ty };
+            }
+          }
+
+          function onMove(e) {
+            if (!pointers.has(e.pointerId)) return;
+            pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
+            if (Math.abs(e.clientX - downX) > TAP_MOVE_TOL || Math.abs(e.clientY - downY) > TAP_MOVE_TOL) moved = true;
+            if (pointers.size === 2 && pinchStartDist > 0) {
+              const [a, b] = [...pointers.values()];
+              const d = dist(a, b);
+              scale = Math.min(MAX, Math.max(MIN, pinchStartScale * (d / pinchStartDist)));
+              clampPan();
+              apply(false);
+            } else if (pointers.size === 1 && panStart && scale > 1) {
+              tx = panStart.tx + (e.clientX - panStart.x);
+              ty = panStart.ty + (e.clientY - panStart.y);
+              clampPan();
+              apply(false);
+            }
+          }
+
+          function zoomToward(clientX, clientY, targetScale) {
+            const rect = img.getBoundingClientRect();
+            const offX = clientX - (rect.left + rect.width / 2);
+            const offY = clientY - (rect.top + rect.height / 2);
+            scale = Math.min(MAX, Math.max(MIN, targetScale));
+            tx = -offX * (scale - 1);
+            ty = -offY * (scale - 1);
+            clampPan();
+            apply(true);
+          }
+
+          function onUp(e) {
+            const wasPinchOrPan = pointers.size >= 1 && (pinchStartDist > 0 || panStart);
+            pointers.delete(e.pointerId);
+            if (pointers.size < 2) pinchStartDist = 0;
+            if (pointers.size === 0) panStart = null;
+
+            if (pointers.size === 0) {
+              if (scale <= 1.02) { reset(true); }
+              else { clampPan(); apply(true); }
+
+              // Double-tap to zoom, only for a genuine tap (no drag/pinch involved).
+              if (!moved && !wasPinchOrPan) {
+                const now = Date.now();
+                const closeEnough = Math.hypot(e.clientX - lastTapX, e.clientY - lastTapY) < 40;
+                if (now - lastTapTime < DOUBLE_TAP_MS && closeEnough) {
+                  if (scale > 1.02) reset(true);
+                  else zoomToward(e.clientX, e.clientY, DOUBLE_TAP_ZOOM);
+                  lastTapTime = 0;
+                } else {
+                  lastTapTime = now; lastTapX = e.clientX; lastTapY = e.clientY;
+                }
+              }
+            }
+          }
+
+          function onCancel(e) {
+            pointers.delete(e.pointerId);
+            if (pointers.size === 0) { panStart = null; pinchStartDist = 0; if (scale <= 1.02) reset(true); }
+          }
+
+          stage.addEventListener('pointerdown', onDown);
+          stage.addEventListener('pointermove', onMove);
+          stage.addEventListener('pointerup', onUp);
+          stage.addEventListener('pointercancel', onCancel);
+
+          return { reset, isZoomed: () => scale > 1.02 };
+        })();
+
         // Tap the photo itself to hide/show the top bar + bottom action bar
-        // (immersive mode) — but never when the tap lands on an actual button.
+        // (immersive mode) — but never when the tap lands on an actual button,
+        // and never while pinch-zoomed/panned (that tap belongs to memZoom).
         document.getElementById('memViewerStage').addEventListener('click', function (e) {
           if (e.target.closest('.mem-viewer-nav')) return;
+          if (memZoom.isZoomed()) return;
           document.getElementById('memViewer').classList.toggle('controls-hidden');
         });
 
         // Swipe left/right between memories, isolated from vertical scroll.
+        // Disabled while zoomed in — at that point single-finger drag pans
+        // the image instead of navigating away from it.
         (function () {
           const stage = document.getElementById('memViewerStage');
           let sx = 0, sy = 0, tracking = false;
           stage.addEventListener('pointerdown', (e) => {
             if (e.target.closest('.mem-viewer-nav')) return;
+            if (memZoom.isZoomed()) return;
             sx = e.clientX; sy = e.clientY; tracking = true;
           });
           stage.addEventListener('pointerup', (e) => {
             if (!tracking) return; tracking = false;
+            if (memZoom.isZoomed()) return;
             const dx = e.clientX - sx, dy = e.clientY - sy;
             if (Math.abs(dx) > 60 && Math.abs(dx) > Math.abs(dy) * 1.5) {
               if (dx < 0) memViewerNext(); else memViewerPrev();
diff --git a/routes/media.js b/routes/media.js
index 9f6578a..b43a8f9 100644
--- a/routes/media.js
+++ b/routes/media.js
@@ -23,9 +23,14 @@ function mediaFileFilter(req, file, cb) {
     cb(null, true);
 }
 
+// 20MB was sized for photos only. Real phone videos routinely exceed that
+// (even a short 15-20s clip is commonly 25-80MB), so videos were being
+// rejected by multer's fileSize limit while photos kept working fine —
+// which is exactly what looked like "video upload doesn't work" from the
+// UI. 150MB comfortably covers a few minutes of phone video.
 const upload = multer({
     storage: multer.memoryStorage(),
-    limits: { fileSize: 20 * 1024 * 1024 }, // 20MB max
+    limits: { fileSize: 150 * 1024 * 1024 }, // 150MB max (covers photos + real phone videos)
     fileFilter: mediaFileFilter
 });
 const uploadAudio = multer({
@@ -33,8 +38,28 @@ const uploadAudio = multer({
     limits: { fileSize: 60 * 1024 * 1024 } // 60MB max for songs
 });
 
+// Multer errors (oversized file, rejected mime type) previously fell through
+// to the app's generic global error handler, which replies with a vague
+// "Internal server error" — the frontend then shows a plain "Upload failed"
+// toast with no indication of why. Handling multer's error here means the
+// real reason (too large / wrong type) reaches the toast instead.
+function handleUploadErrors(uploadMiddleware) {
+    return (req, res, next) => {
+        uploadMiddleware(req, res, (err) => {
+            if (!err) return next();
+            if (err.code === 'LIMIT_FILE_SIZE') {
+                return res.status(413).json({ error: 'That file is too large to upload (max 150MB).' });
+            }
+            if (err.message === 'Unsupported file type') {
+                return res.status(415).json({ error: 'That file type isn\'t supported — please choose a photo or video.' });
+            }
+            return res.status(400).json({ error: err.message || 'Upload failed' });
+        });
+    };
+}
+
 // ── POST /api/media/upload ─────────────────────────────
-router.post('/upload', upload.single('file'), async (req, res) => {
+router.post('/upload', handleUploadErrors(upload.single('file')), async (req, res) => {
     if (!req.file) return res.status(400).json({ error: 'No file provided' });
     const { coupleId, type } = req.body;
     if (!coupleId) return res.status(400).json({ error: 'coupleId required' });
