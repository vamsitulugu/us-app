diff --git a/public/chat/chat.css b/public/chat/chat.css
index 7caeaf8..8c24ca8 100644
--- a/public/chat/chat.css
+++ b/public/chat/chat.css
@@ -628,15 +628,33 @@
    first, this only kicks in when a word alone is wider than the bubble. */
 .chat-text{white-space:pre-wrap;overflow-wrap:break-word;word-break:normal;overflow-wrap:anywhere;line-height:1.44;margin:0}
 .chat-meta{display:flex;gap:4px;align-items:center;justify-content:flex-end;font-size:10px;opacity:.7;margin-top:3px;font-variant-numeric:tabular-nums}
-.chat-status{font-size:13px;line-height:1;letter-spacing:-1.5px}
-.chat-status.read{color:#7dd3fc}
+/* Message status ticks — proper vector icons (SVG), never Unicode/emoji
+   checkmarks. Color alone communicates state: muted for sent/delivered,
+   WhatsApp blue only once the partner has actually read the message. */
+.chat-ticks{display:inline-flex;align-items:center;line-height:0;color:rgba(255,255,255,.62);transition:color .25s ease}
+.chat-ticks.sending{color:rgba(255,255,255,.45)}
+.chat-ticks.read{color:#53bdeb}
 .chat-reactions{display:flex;gap:3px;flex-wrap:wrap;margin-top:4px}
 .chat-reaction-pill{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.1);border-radius:11px;padding:1px 7px 2px;font-size:11.5px;line-height:1.5;backdrop-filter:blur(4px)}
 .chat-bubble.mine .chat-reaction-pill{background:rgba(0,0,0,.18);border-color:rgba(255,255,255,.15)}
 .chat-img{max-width:230px;border-radius:14px;display:block;margin-bottom:4px}
 .chat-gif{max-width:190px}
+/* ── SYSTEM CHAT EVENTS ──
+   Shared look for anything that isn't a real sent/received message: date
+   separators, call-history entries, routine/activity notices, blocked-user
+   notices, etc. Always a compact centered dark pill sitting on top of the
+   wallpaper — the wallpaper itself is never touched by these rules. */
+.chat-system-event{
+  display:flex; align-items:center; justify-content:center; gap:6px;
+  text-align:center; margin:14px auto; max-width:86%; width:fit-content;
+  padding:5px 14px; border-radius:14px;
+  background:rgba(10,10,14,.62); border:1px solid rgba(255,255,255,.08);
+  box-shadow:0 2px 10px rgba(0,0,0,.25);
+  -webkit-backdrop-filter:blur(6px); backdrop-filter:blur(6px);
+  font-size:11px; color:rgba(255,255,255,.72); line-height:1.35;
+}
 .chat-date-sep{text-align:center;margin:16px 0 10px;font-size:10.5px;color:var(--text3);text-transform:uppercase;letter-spacing:.6px}
-.chat-date-sep span{background:rgba(255,255,255,.06);border:1px solid var(--border);border-radius:12px;padding:3px 12px}
+.chat-date-sep span{background:rgba(10,10,14,.62);border:1px solid rgba(255,255,255,.08);border-radius:14px;padding:4px 14px;box-shadow:0 2px 10px rgba(0,0,0,.25);-webkit-backdrop-filter:blur(6px);backdrop-filter:blur(6px)}
 /* Legacy .chat-jump-btn / .chat-jump-badge rules removed — they duplicated
    and fought with the canonical, animated definitions above (line ~236),
    forcing an abrupt display:none/flex toggle at a different `bottom` offset
@@ -729,7 +747,9 @@
 .chat-gif-sheet input{width:100%;padding:10px 14px;border-radius:14px;border:1px solid var(--border);background:rgba(255,255,255,.05);color:#fff;margin-bottom:10px}
 .chat-gif-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;max-height:340px;overflow-y:auto}
 .chat-gif-grid img{width:100%;border-radius:10px;cursor:pointer}
-.chat-call-log{text-align:center;font-size:11px;color:var(--text3);padding:6px 0;display:flex;justify-content:center;gap:6px}
+.chat-call-log{text-align:center;font-size:11px;color:rgba(255,255,255,.72);margin:10px auto;max-width:86%;width:fit-content;padding:6px 14px;border-radius:14px;display:flex;align-items:center;justify-content:center;gap:7px;background:rgba(10,10,14,.62);border:1px solid rgba(255,255,255,.08);box-shadow:0 2px 10px rgba(0,0,0,.25);-webkit-backdrop-filter:blur(6px);backdrop-filter:blur(6px)}
+.chat-call-log svg{flex-shrink:0;opacity:.85}
+.chat-call-time{opacity:.6;font-variant-numeric:tabular-nums}
 .deleted-bubble{opacity:.5;font-style:italic}
 .flash{animation:flashHighlight 1.2s ease}
 @keyframes flashHighlight{0%,100%{background:transparent}30%{background:rgba(229,9,20,.2)}}
diff --git a/public/chat/chat.js b/public/chat/chat.js
index 4e93973..64518f3 100644
--- a/public/chat/chat.js
+++ b/public/chat/chat.js
@@ -632,6 +632,33 @@ function reanchorAfterImages() {
     return 'mid';
   }
 
+  // WhatsApp-style message status indicator, rendered as inline SVG (never
+  // Unicode/emoji characters). Reads the real backend state on the message
+  // (delivered / delivered_at / read / read_at) — a 'temp_' id means the
+  // optimistic bubble hasn't been confirmed by the server yet, i.e. SENDING.
+  // Two-check icons are shared markup; only the wrapper class/color differs
+  // between delivered (muted) and read (blue), so CSS alone drives the
+  // color swap and can transition it smoothly.
+  function renderTicks(m) {
+    const sending = typeof m.id === 'string' && m.id.indexOf('temp_') === 0;
+    if (sending) {
+      return `<span class="chat-ticks sending" title="Sending">
+        <svg viewBox="0 0 16 16" width="14" height="14" fill="none"><circle cx="8" cy="8" r="6.2" stroke="currentColor" stroke-width="1.4" opacity=".55"/><path d="M8 4.4V8l2.6 1.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" opacity=".85"/></svg>
+      </span>`;
+    }
+    if (!m.delivered && !m.read) {
+      // SENT — single tick
+      return `<span class="chat-ticks sent" title="Sent">${TICK_SVG_SINGLE}</span>`;
+    }
+    if (m.read) {
+      return `<span class="chat-ticks read" title="Read">${TICK_SVG_DOUBLE}</span>`;
+    }
+    // DELIVERED — two muted ticks
+    return `<span class="chat-ticks delivered" title="Delivered">${TICK_SVG_DOUBLE}</span>`;
+  }
+  const TICK_SVG_SINGLE = `<svg viewBox="0 0 16 11" width="15" height="10" fill="none"><path d="M1 5.6 5 9.6 15 1" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>`;
+  const TICK_SVG_DOUBLE = `<svg viewBox="0 0 20 11" width="18" height="10" fill="none"><path d="M1 5.6 5 9.6 15 1" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/><path d="M6 5.6 10 9.6 20 1" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>`;
+
   function renderBubble(m, isNew, groupPos) {
     if (m.deleted) {
       return `<div class="chat-row ${isMine(m) ? 'me' : 'them'}"><div class="chat-bubble deleted-bubble">🚫 Message deleted</div></div>`;
@@ -668,14 +695,13 @@ function reanchorAfterImages() {
         ${opts.map(o => `<div class="msg-poll-opt" onclick="event.stopPropagation();Chat.votePoll(${m.id},'${esc(o).replace(/'/g,"\\'")}')">${esc(o)}</div>`).join('')}
       </div>`;
     }
-    else if (m.type === 'call_log') return `<div class="chat-call-log${isNew ? ' msg-pop-in' : ''}"><span>${esc(m.text)}</span><span class="chat-call-time">${time}</span></div>`;
+    else if (m.type === 'call_log') return `<div class="chat-call-log chat-system-event${isNew ? ' msg-pop-in' : ''}"><svg viewBox="0 0 16 16" width="12" height="12" fill="currentColor"><path d="M3.6 1.4c.5-.2 1.1 0 1.3.5l1 2.2c.2.4.1.9-.2 1.2l-1 1c.6 1.5 1.8 2.7 3.3 3.3l1-1c.3-.3.8-.4 1.2-.2l2.2 1c.5.2.7.8.5 1.3l-.6 1.5c-.2.5-.7.8-1.2.7-4.6-.6-8.3-4.3-8.9-8.9-.1-.5.2-1 .7-1.2z"/></svg><span>${esc(m.text)}</span><span class="chat-call-time">${time}</span></div>`;
     else body = `<div class="chat-text">${linkify(esc(m.text || ''))}</div>${renderLinkPreview(m.text)}`;
 
     const reactions = m.reactions && Object.keys(m.reactions).length
       ? `<div class="chat-reactions">${Object.entries(m.reactions).map(([e, roles]) => `<span class="chat-reaction-pill">${e} ${roles.length}</span>`).join('')}</div>` : '';
 
-    const status = mine ? (m.read ? '✓✓' : m.delivered ? '✓✓' : '✓') : '';
-    const statusClass = mine && m.read ? 'read' : '';
+    const status = mine ? renderTicks(m) : '';
 
     const quoted = m.reply_to ? renderQuote(m.reply_to) : '';
 
@@ -685,7 +711,7 @@ function reanchorAfterImages() {
         ${quoted}
         ${body}
         ${reactions}
-        <div class="chat-meta"><span>${time}${m.edited ? ' · edited' : ''}</span>${mine ? `<span class="chat-status ${statusClass}">${status}</span>` : ''}</div>
+        <div class="chat-meta"><span>${time}${m.edited ? ' · edited' : ''}</span>${mine ? status : ''}</div>
       </div>
     </div>`;
   }
