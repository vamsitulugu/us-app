diff --git a/public/index.html b/public/index.html
index 1b604f1..1e34fe4 100644
--- a/public/index.html
+++ b/public/index.html
@@ -7301,6 +7301,12 @@ select option { background: #0D0D0D; color: #fff; }
   </div>
 </div>
 
+<div class="page" id="page-dailyroutine">
+  <div class="iframe-page-wrap">
+    <iframe id="dailyroutineFrame" src="/daily-routine.html" title="Daily Routine" allow="autoplay" loading="lazy"></iframe>
+  </div>
+</div>
+
       <!-- ════ CATEGORY NAV PAGES ════ -->
       <div class="page cat-page" id="page-catUs">
         <div class="cat-page-header">
@@ -7344,6 +7350,7 @@ select option { background: #0D0D0D; color: #fff; }
           <div class="cat-row" onclick="goto('calendar')"><span class="cat-row-ico">📅</span><span class="cat-row-title">Calendar</span><span class="cat-row-chev">›</span></div>
           <div class="cat-row" onclick="goto('bucket')"><span class="cat-row-ico">🌟</span><span class="cat-row-title">Bucket List</span><span class="cat-row-chev">›</span></div>
           <div class="cat-row" onclick="goto('meetplanner')"><span class="cat-row-ico">💕</span><span class="cat-row-title">Meet Planner</span><span class="cat-row-chev">›</span></div>
+          <div class="cat-row" onclick="goto('dailyroutine')"><span class="cat-row-ico">🗓️</span><span class="cat-row-title">Daily Routine</span><span class="cat-row-chev">›</span></div>
           <div class="cat-row" onclick="goto('surprise')"><span class="cat-row-ico">🎁</span><span class="cat-row-title">Surprises</span><span class="cat-row-chev">›</span></div>
           <div class="cat-row" onclick="goto('money')"><span class="cat-row-ico">💰</span><span class="cat-row-title">Money</span><span class="cat-row-chev">›</span></div>
         </div>
@@ -8373,6 +8380,7 @@ function setJustPairedAt(ts) {
           music: 'Music ', capsule: 'Love Capsule ', surprise: 'Surprises ',chat: 'Chat ',
           level: 'Couple Level ',globe: 'Memory Globe ', dreamhome: 'Dream Home ',games: 'Couple Games ',
           dreamgoals: 'Dream Goals ', places: 'Places Memory ', meetplanner: 'Meet Planner ',
+          dailyroutine: 'Daily Routine ',
           catUs: 'Us', catMemories: 'Memories', catPlans: 'Plans', catTogether: 'Together',
           catFuture: 'Our Future', catMyspace: 'My Space'
         };
@@ -8383,6 +8391,7 @@ function setJustPairedAt(ts) {
           profile: 'catUs', lovecounter: 'catUs', level: 'catUs', fights: 'catUs',
           camera: 'catMemories', globe: 'catMemories', places: 'catMemories', collection: 'catMemories', capsule: 'catMemories',
           calendar: 'catPlans', bucket: 'catPlans', meetplanner: 'catPlans', surprise: 'catPlans', money: 'catPlans',
+          dailyroutine: 'catPlans',
           music: 'catTogether', games: 'catTogether', movie: 'catTogether', study: 'catTogether',
           dreamgoals: 'catFuture', dreamhome: 'catFuture', virtualhome: 'catFuture',
           myspace: 'catMyspace', period: 'catMyspace', vault: 'catMyspace'
@@ -8412,7 +8421,7 @@ function setJustPairedAt(ts) {
         // in the DOM) — used to sanity-check both the initial ?page= URL param
         // and any notification-click payload before calling goto() with it.
         const DEEP_LINK_PAGES = new Set([
-          'dashboard', 'chat', 'music', 'globe', 'virtualhome', 'meetplanner',
+          'dashboard', 'chat', 'music', 'globe', 'virtualhome', 'meetplanner', 'dailyroutine',
           'games', 'calendar', 'settings', 'profile', 'map', 'ai', 'capsule',
           'surprise', 'level', 'dreamhome', 'dreamgoals', 'places',
           'lovecounter', 'fights', 'myspace', 'vault', 'bucket', 'money',
@@ -8556,7 +8565,7 @@ function setJustPairedAt(ts) {
             const FRAME_IDS = {
               collection: 'collectionFrame', globe: 'globeFrame', music: 'musicFrame',
               lovecounter: 'lovecounterFrame', dreamgoals: 'dreamgoalsFrame',
-              places: 'placesFrame', meetplanner: 'meetplannerFrame',
+              places: 'placesFrame', meetplanner: 'meetplannerFrame', dailyroutine: 'dailyroutineFrame',
               games: 'gamesFrame', virtualhome: 'virtualhomeFrame', movie: 'movieFrame'
             };
             const fid = FRAME_IDS[page];
@@ -8653,6 +8662,7 @@ function setJustPairedAt(ts) {
           if (page === 'dreamgoals') { syncThemeToFrame('dreamgoalsFrame'); syncDreamGoalsToFrame(); }
           if (page === 'places') { syncThemeToFrame('placesFrame'); syncPlacesMemoryToFrame(); }
           if (page === 'meetplanner') { syncThemeToFrame('meetplannerFrame'); syncMeetPlannerToFrame(); }
+          if (page === 'dailyroutine') { syncThemeToFrame('dailyroutineFrame'); syncDailyRoutineToFrame(); }
           if (page === 'virtualhome') {
             // Load the Home world only when actually entering it — its 3D scene,
             // Realtime channel, and sync timers all live inside this iframe, so
@@ -13284,6 +13294,17 @@ document.addEventListener('keydown', e => {
           if (frame.conten4tDocument && frame.contentDocument.readyState === 'complete') doIt();
           else frame.addEventListener('load', doIt, { once: true });
         }
+        function syncDailyRoutineToFrame() {
+          const frame = document.getElementById('dailyroutineFrame');
+          if (!frame) return;
+          const doIt = () => {
+            try {
+              frame.contentWindow.postMessage({ type: 'names', my: S.myName || 'You', partner: S.partnerName || 'Partner' }, '*');
+            } catch(e) {}
+          };
+          if (frame.contentDocument && frame.contentDocument.readyState === 'complete') doIt();
+          else frame.addEventListener('load', doIt, { once: true });
+        }
         window.addEventListener('message', function(e) {
           if (e.data && e.data.type === 'dreamgoals') { S.dreamGoals = e.data.dreams || []; scheduleSave(); }
           if (e.data && e.data.type === 'placesmemory') { S.placesMemories = e.data.places || []; scheduleSave(); }
@@ -13296,7 +13317,7 @@ document.addEventListener('keydown', e => {
         });
         // Patch setTheme to sync iframes
         const _origST = window.setTheme;
-        window.setTheme = function(n, s) { _origST && _origST(n, s); setTimeout(() => ['gamesFrame','musicFrame','dreamgoalsFrame','placesFrame','lovecounterFrame','collectionFrame','meetplannerFrame','movieFrame'].forEach(syncThemeToFrame), 150); };
+        window.setTheme = function(n, s) { _origST && _origST(n, s); setTimeout(() => ['gamesFrame','musicFrame','dreamgoalsFrame','placesFrame','lovecounterFrame','collectionFrame','meetplannerFrame','movieFrame','dailyroutineFrame'].forEach(syncThemeToFrame), 150); };
 
         async function setupNativeNotifications() {
   if (!window.Capacitor?.isNativePlatform?.()) { console.log('[fcm] skip — not running inside the native app'); return; }
diff --git a/server.js b/server.js
index 06a43c4..96adac6 100644
--- a/server.js
+++ b/server.js
@@ -127,6 +127,7 @@ app.use('/api/search', require('./routes/search'));
 app.use('/api/presence', require('./routes/presence'));
 app.use('/api/movie', require('./routes/routes-movie'));
 app.use('/api/meetplanner', require('./routes/meetplanner'));
+app.use('/api/daily-routine', require('./routes/daily-routine'));
 
 // ── Public release/flag endpoints (no auth — called by the app itself) ─
 app.use('/api/releases', require('./routes/releases'));
